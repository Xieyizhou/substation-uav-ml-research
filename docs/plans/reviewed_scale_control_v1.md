# 冻结缩放增强对照：进入真实预检前

尺度探针十图、六模型结果：原尺寸重点目标 60/60，0.75 倍 46/60，0.5 倍 39/60；完整实例匹配依次为 162/162、142/162、103/162。该结果支持检验缩放增强，不认证真实距离因果效应。

训练使用独立 `scale-padding-control-v1`。保留两个家族和全部 seed 7/17/27，共六份新终点；各自对照为刚完成训练的 reviewed ISR/ISM 权重，不按本次诊断选有利 seed。所有源成员、位置、6,600 次曝光、完整类别实例曝光、亮度数组、v2.11 初始化和 1,100 优化步均保持不变。

唯一策略变化：每 50 步 300 次曝光中，150 次原尺寸、75 次 0.75 倍、75 次 0.5 倍，按版本、seed、位置哈希冻结顺序；两家族同 seed 的缩放参数相同。整图双线性缩放后居中填充 RGB 114，不裁剪；同步缩放平移全部标签，无选择性删除。负例位置保持不变，但同样应用增强，这是实验因素的一部分。亮度处理在先，缩放在后。

CPU 2 并发 × 4 线程，640，batch/nbs=6，AdamW，恒定 LR=0.0005，无预热，其他增强关闭。源图和历史标签不修改。评估及保留门槛保持原协议；只使用预定终点权重。不因未改善而自动追加配置。

下一步实现独立运行时并对六套序列逐次验证 39,600 次实际加载、全部标签几何变换和图像张量；不得创建优化器或执行反向传播。缩放 1 的图像和标签应在允许的数值精度下与参考一致，标签数量及类别实例必须严格一致。退化或空框不能被静默过滤。

技术记录：探针首次遇到历史 YOLO 小数坐标重建为 −9.6e−8 像素边界，属舍入误差。工具只允许 1e−6 像素以内的浮点边界误差且保留坐标，不修改标签、不接受实质越界。对应回归已补充。

当前只有设计冻结；真实加载预检尚未执行，训练尚未开始。通过后按用户授权自动启动；所有新产物 training_admitted=false、promotable=false。该对照测试的是缩放、插值、填充整体策略，不声称纯尺度或跨场景泛化。
