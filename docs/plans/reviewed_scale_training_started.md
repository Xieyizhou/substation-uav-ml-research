# 缩放增强对照已启动

六个配置的完整预检均通过：共 39,600 次实际加载、6,600 个 batch，核对成员顺序、冻结亮度及缩放参数、完整标签几何和最终图像／监督张量。针对性回归 25 项通过，v2.11 固定 40 文件完整性通过；未声明全仓测试通过。

独立实验目录是本轮 `reviewed-dataset-v1/exposure-control-v1/scale-padding-control-v1`。`entry-ready.json` 记录启动前就绪状态，不回写；真实启动情况见 `training-runner` 下单元日志及 `training` 产物。

已启动 seed 7 的 ISR1100、ISM1100 两个单元，两者日志均已进入第 2/110 epoch，出现实际训练批次及 box/cls/dfl 损失。启动目标因此已达到，不是仅创建进程。运行器保持 2 并发 × 每进程 4 CPU 线程，随后自动接续 seed 17、27，共六个单元。

每单元 1,100 优化步、6,600 次曝光，成员、顺序、亮度系数、初始化和预算不变，仅加入冻结的缩放／插值／填充策略。风险图不恢复，标签不修改。训练每个 batch 的图像和监督张量必须与预检哈希相同，否则失败停止。

当前仅确认训练启动，不声称训练完成、模型改善或评估门禁通过。训练结束将保留终点权重和回执；该运行入口没有自动调用开发评估，不能把历史自动评估意向误报为已经接入。所有新产物继续 training_admitted=false、promotable=false。
