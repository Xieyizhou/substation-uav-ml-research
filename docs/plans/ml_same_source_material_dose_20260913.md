# 同源材质曝光30→45：执行边界

按用户确认方案执行。独立目录same-source-material-dose-control-v1，保留历史实验、标签及审核。48成员136个标签复用有效逐框AI辅助证据，原始PPM与导出PNG仅在原尺寸RGB逐像素一致时对应。初版quality.json仅按文件哈希匹配产生34项缺口；quality-v2.json解决该技术格式差异，初检保留，不宣称改过历史审核。

三个D组从v2.11独立初始化，对照有效T-7/17/27。固定2700次曝光、450步、CPU640、batch/nbs6、AdamW、LR0.0005恒定无预热，仅冻结亮度。15个原始槽位换同源warm/cool/gray_target_body各5次，最终各15次。SHA256版本排序+动态规划固定解，S07新增四次按2/1/1分配，不依据模型表现。允许原始成员零曝光，但来源/完整监督不变；其余2685个位置与负例固定。保持training_admitted=false、promotable=false。

质量缺口、分配不可行、实际监督变化、参考失效均停止，不放宽剂量或换成员。预检禁止优化器和反向传播；通过才自动训练三seed。每单元最多三次技术尝试，语义错误停止、异常清理进程；不拼接未知状态。

训练结束自动进行96张固定开发图双阈值0.37/0.001推理，CPU640、同类NMS0.7、max_det300、匹配IoU≥0.5，完整继承数值及能力保留门槛。之后逐框审核全部无目标误检及原始/光照新增漏检，保留完整得失；84张同源图拟合必须区分共同已曝光、新增曝光、零曝光原始图，13个历史丢失事件只作辅助队列。全部三seed完成后判断，不选best，不根据单seed中断。

最终交付中文报告和身份回执；不补采、不解封新场景、不接入飞行、不自动开始后续实验。无论通过、失败或质量阻断，均报告有边界判断。
