# 训练集构建前：十二权重拟合与来源核验

状态：分析与证据链接进行中，训练集未就绪。目标仍是获得可核验、可用于下一轮有界开发训练的数据集，而不只是完成推理。未训练、未修改历史标签、未访问保护标签；training_admitted=false、promotable=false。

## 已完成的实证

429张冻结池成员，十二份1100步终点权重，分别以confidence 0.37及0.001进行CPU640推理。各模型按实际曝光区分训练拟合与未曝光成员，完整标签一对一匹配，不能用计划类别代替全图监督。十二单元均完成且通过结果身份及重算匹配验证。

| 已曝光训练成员指标 | ISR三个seed范围 | ISM三个seed范围 |
| --- | ---: | ---: |
| 电抗器召回 | 98%–100% | 100% |
| 电容器召回 | 90.48%–95.24% | 94.03%–98.51% |
| 开关柜召回 | 93.28%–99.16% | 96.85%–98.43% |
| 变压器召回 | 95.26%–98.42% | 97%–98% |
| 108张已曝光无目标图的误报帧数 | 0–3 | 0–1 |

相应原分块SR组无目标训练图误报52–63帧，SM组40–75帧。交错顺序使训练拟合与负例抑制明显改善，但此前开发审核仍发现清楚电抗器的漏检和能力保留失败。因此支持条件迁移/来源覆盖受限的调查方向，不能凭这些汇总确认唯一机制，亦不能宣称整个训练池已学会。

## 数据身份核对

425张与登记source_image像素一致。4张physical-lighting图的该字段实际指向派生母图；已从原导出脚本追到light-review/evidence.json及实际重放receipt和RGB，与当前导出图像素一致，并核验原有11项全标签审核。新增来源解析记录保留母图与实际渲染图两种关系，没有修改历史字段。

3组相同像素变体（G03、S05、S06的gray_target_body/gray_all_body）完整标签一致，属于相同登记谱系。未来清单需登记像素等价关系，不能作为独立覆盖膨胀；不删除历史成员。

64张近期small/cool/gray候选已通过各自既有审核验证器，匹配当前导出图与原始RGB、全部审核事件和标签数量。原限制继续保留，包括部分遮挡、背面和共享资产；这些证据不等于通用训练准入。

## 尚需完成

### 本次完整补审进度（2026-09-15）

对没有严格既有审核链接的51张当前曝光成员，已逐图查看全图及每个真值自己的裁剪，记录164个标签：58项内容可见、98项内容有限、8项内容不足。所有观察均为AI辅助审核，绑定当前图像、完整标签、证据页哈希及时间，不认证像素级实例可见性，也不自动取得训练资格。

8项内容不足分布于U22、U28、U31、U45、U47、U49、U50七张图：包括右缘仅剩极窄片段，以及后排柜体几乎被前景设备覆盖。独立quality-isolation-v2.json将这七张整图隔离，未删单标签、未修改历史文件。429成员的当前账本为42张隔离、4张待定、383张仍需完整质量与角色门禁；其中283张属于当前模型实际曝光成员并尚未隔离/暂缓。这不是“283张已可训练”。

背面、封闭箱体和局部遮挡的98项仅作为描述性限制。不能把没有内部组件可见性等同于已确认错误标签，也不能为缩小工作量直接全部丢弃。必须结合已经接受的封闭外观目标定义、完整标签、来源身份及全图覆盖形成最终处置。

新增legacy-world-source-trace.json对116张能在既有来源清单定位的成员完成原始RGB像素、完整标签坐标/类别及保存世界实例编号的对应；标注设备名称来自各自原始实例编号，不使用计划目标代替。这是事后追溯，历史回执中缺少实际模式和映射的事实仍保留，不补称原采集通过新门禁。

U01/U02初看出现未框蓝色柜状物，当前仍不能确认为漏标：保存世界包含未赋目标标签的cabinet_1/2/3和赋予149标签的switchgear_south，其外形接近。缺少标签本身不能证明背景语义；还需空间对应和来源类别定义核对。类似具名未框结构已写入各帧观察，不能静默放行，也不能直接据颜色重标。

相关15项定向回归已通过，覆盖缺失/重复/过期审核、完整标签遗漏、非计划设备实例解析、坐标/类别变化、世界实例碰撞、整图同源风险传播及历史暂缓不恢复。不声明全仓测试通过。

本次另复核v2.11固定40文件完整性通过。该核验只覆盖固定模型文件和成员元数据，不代表重新审核了保护集图像或标签，也不代表新数据集已就绪。

本次新入口：trace_order_fit_legacy_worlds、record_order_fit_remaining_review、extend_order_fit_quality_isolation。仅读取历史输入、写入独立诊断产物，不启动训练。

### 余下验收

1. 将拟用的基础、常规、桥接、负例及更早新来源逐项链接到最新有效审核与暂缓记录；文件哈希正确或曾训练过不能替代内容决定。
2. 对照已审核开发失败结构，完成已曝光训练成员的具名覆盖核对；未知来源不计作独立来源。
3. 冻结完整可用/暂缓/像素等价清单及角色隔离证据。不能只挑预测正确样本或有利seed。
4. 以原图和完整原标签导出独立版本，验证真实加载、完整标签、重复关系、开发/保护成员排除及恢复哈希，再签发有界开发训练就绪回执。

当前不启动训练；若监督冲突仍未解决，继续追溯或给出具名隔离理由，不能通过模型分数替代质量门禁。
