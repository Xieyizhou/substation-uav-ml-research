# 整图暂缓与固定预算替换对照：训练前设计

日期：2026-09-09。设计目录 `whole-image-hold-control-design-v1`。
状态：`design_ready_execution_not_authorized`，不是训练就绪回执。

## 已确认依据与边界

权限恢复批次的合并提案建议暂缓 T08/T29/T30/T33；T04/T26/T32 仅补充元数据语义说明。T08/T30 有原帧像素完全对齐的实例证据，T29/T33 依旧依据原图与来源审核，不能借用前者的掩码认证。

暂缓理由是可辨识内容不足，不是证明目标不存在或 full_2d 坐标错误。旧 `not_truncated` 表示转换器未裁剪坐标，不认证物理完整可见。本方案不修改原图、完整标签、旧审核或模型；不采集、不训练、不读取保护标签、不解封新场景、不使用定时或跨轮跟踪任务。所有新产物保持 `training_admitted=false`、`promotable=false`。

## 独立版本提案

在后续获准实施时，以原236张清单生成只读引用式的新版本，暂缓整张图及完整标签，不移动或删除原文件，不删框留图，不用可见掩码框替代 full_2d，不引入未经实现验证的 ignore 标签。

四图含4个电抗器、7个变压器、3个开关柜和3个电容器组标签。按当前登记闭包，暂缓后候选清单为232张、388个完整标签，其中31个电抗器标签；这不是新池已获准入。基础池62→61、常规正样本39→36，桥接正样本15、困难负例120不变。

实施前重新核对布局、资产、实例、位姿及派生组闭包。现有登记未扩出其他成员，但不能证明不存在未知同源关系。发现新关系或标签/身份冲突时阻断、更新独立方案，不静默扩大范围。三项元数据说明采用旁路勘误，原字段不覆盖。审核来源统一保留 AI 辅助审核性质。

## 推荐唯一模型对照：整图暂缓加同子集替换策略

两个组各 seed 7/17/27，共六个单元，从 v2.11 独立初始化。共同使用当前交错组的450步、2700次曝光为模板；CPU、640、batch/nbs 6、AdamW、学习率0.001、恒定学习率、无预热、增强关闭，只评估预定终点。

- 参考组：完整复现现有交错曝光顺序。
- 暂缓策略组：只在四图原有曝光位置，以同子集的合格保留成员替换；其余位置、批内顺序和困难负例曝光完全不变。

不把删除曝光后少训练若干步当作同预算对照。替换确实改变其他成员和类别实例曝光，因此解释范围是“整图暂缓加同子集替换这一数据策略”的效果，不是纯粹监督质量效应，也不是仅移除电抗器标签的因果效应。

后续精确序列生成规则：候选成员须通过完整标签、来源及既有审核核验，排除暂缓闭包和具名待定；按 member_id 排序，再用带版本、seed、子集、循环编号的 SHA-256 排序键生成循环排列。同子集曝光空位按原序填充，循环耗尽再进入下一循环。替换不按模型成绩、类别或人工偏好挑选；不同 seed 均保留。候选资格规则和集合先冻结，集合为空则阻断。当前步骤不生成替换序列或导出训练池。

| seed | 待替换曝光 | 基础池位 | 常规位 | 受影响批次数 |
|---|---:|---:|---:|---:|
| 7 | 142 | 34 | 108 | 132 |
| 17 | 141 | 36 | 105 | 119 |
| 27 | 142 | 35 | 107 | 127 |

冻结前报告每张图、每类完整实例、已登记谱系和每50步窗口的替换前后曝光差，不引入类别重采样。新数据策略不能声称两组实例曝光相同。历史交错权重可作旁证；若要替代重新训练的参考组，必须另行证明代码、环境、初始化、实际采样及全部控制相同，不能默认复用。

## 训练前门禁与评估

实施首先交付独立版本清单、暂缓/勘误账本、六个精确序列、完整实例曝光差及输入哈希。真实加载器逐一遍历2700次曝光并验证完整标签，不创建优化器、不反向传播。验证原组序列完全复现、策略组仅指定位置变化、无暂缓成员泄漏、同子集替换、困难负例及其他位置不变、确定性可复算；缺失、重复或过期审核及恢复产物拒绝。默认入口不得训练。相关回归及固定40文件完整性核验通过后，才能签发 `ready_for_training_not_started`；显式训练授权另行确认。

固定已有48张配对开发图和48张无目标开发图，不改变分母或角色。正式推理confidence 0.37，诊断0.001；CPU、640、类别相关NMS IoU 0.7、max_det 300、同类一对一匹配IoU≥0.5。计划目标与全图分配冲突必须解释。按原始/材质/背景/光照、全图及逐类、seed和pair_id报告得失；不把seed重复算新样本。

沿用原始计划命中均值≥0.833333/最差seed≥0.75；材质≥0.5/最差≥0.333333；背景、光照均值≥0.6；无目标FPR均值≤0.1/最差≤0.2。原始及光照的全图和逐类召回，相对同预算 retained_reference-450 与历史A的三seed均值都不得下降超过0.05。交错参考组用于数据策略比较，不能替代这两个能力保留参考。全部评估身份与精确数值规则需在实施协议中绑定。

无论达标与否，都报告四图连带移出其他类别监督的影响。改善仅支持该数据策略在已查看开发条件下有效；不证明唯一根因、不晋升、不自动扩大暂缓或追加有利seed。

## 下一判断点

优先处理已具名监督风险，而非立刻增加步数或更换模型结构。当前可以进入独立版本和替换序列的训练前实施，但尚不能开始训练。身份未知、752项细结构待确认和开发条件迁移问题不会因暂缓四图自动解决。
