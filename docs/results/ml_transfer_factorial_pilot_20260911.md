# 同位姿材质迁移先导诊断

## 已完成

20 张重放图的 60 个标签已记录 AI 辅助逐框观察；不认证原帧像素可见性，不自动收录训练。
加上既有 8 张暖／冷变体，共 28 张图，12 份既有权重均完成 confidence 0.37 和 0.001 的独立推理。只有 4 个来源位姿，重复变体和 seed 不增加独立样本数。

冻结协议及逐实例结果位于 `data/research/ml_training_recovery_v1/material-view-candidates-v1/compensated-material-candidates-v1/condition-transfer-route-v1/factorial-probe-v1/pilot-v1/inference-v2/`。

## 具体线索

全部 12 模型在 4 个原始计划目标上均命中。高曝光 VM 的 seed 17、27 在 G02 的 `west_switchgear_01` 上：仅主体变灰时命中，主体和面板一起变灰时漏检；低阈值下均有合格同类框，操作性归类为低置信度，不存在正式一对一匹配竞争。

这支持将面板对比度变化作为一个独立训练条件候选，但不能认定其为唯一根因：只有一个开关柜位姿，seed 7 没有同样的计划命中损失；其他类别的目标无独立面板，相应条件是结构性无变化，不能充当面板效应的独立复现。

高曝光 VM 的 seed 27 在 G01 灰色电容器主体上已有低置信度漏检，加入面板条件没有改变该结果。不能用开关柜面板线索解释全部类别。

## 技术勘误及边界

首次推理入口将实例解析的 `category` 直接交给需要 `class_name` 的匹配器，触发 KeyError，未形成完整模型结果。失败尝试保留于 inference-v1。独立 v2 恢复原始完整真值并附加唯一设备 ID；没有更改历史标签、条件或阈值。

13 项相关单元回归通过；额外对冻结 28 张图逐一执行完整真值／空预测匹配核验通过。不宣称全仓测试或本阶段全部门禁通过。

## 下一步

先完成全图来源审计，并扩展到协议预定的其余 8 个位姿，检查面板与周围设备材质效应是否复现，再决定固定总曝光下的条件替换方案。尚未完成扩大审核、来源隔离、曝光可行性及真实加载器预检，因此尚不能宣布训练路线就绪。未启动新训练；所有产物不具备正式训练准入和晋升资格。
