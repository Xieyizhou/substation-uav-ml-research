# 风险图不增曝约束：精确配额可行性核对

2026-09-09，独立目录 `risk-capped-redistribution-feasibility-v1`。仅输出计数方案及审计，不生成训练序列，不训练。所有产物 `training_admitted=false`、`promotable=false`；历史标签、协议与权重保持原样。

## 结论

三个 seed 均找到并直接整数复核了可行解。十二张风险图不增加曝光与原定四类实例/子集精确曝光约束可以同时满足，不需要为凑配额放行风险图。

上限基准是每个 seed 原始 `reference-450` 序列，而不是上一份未获质量批准的重分配方案。四张原定暂缓图仍为零，其余 112 张正样本至少一次；每张负例计数保持不变。

| 项目 | seed 7 | seed 17 | seed 27 |
|---|---:|---:|---:|
| 风险 12 图总曝光，原来→新方案 | 150→150 | 151→151 | 150→150 |
| 单张正样本最大新增曝光 | 7 | 7 | 7 |
| 增加曝光的成员数 | 37 | 38 | 37 |
| 对应计数变化至少需替换的位置数 | 250 | 249 | 252 |
| 单张图总曝光最大值 | 117 | 117 | 117 |

十二张风险图在当前解中逐张计数均未变化。每个 seed 的子集曝光均为基础 972、常规 702、桥接正例 540、负例 486，共 2700；完整标签的四类实例曝光与参考逐类精确相等。负例位置尚未生成，不能把计数相同写成实际序列已验证。

求解先最小化单图最大新增曝光，再在该最优上限下最小化保留正样本计数绝对变化；两阶段求解差距均为零，输出经直接整数代入复核。“至少替换位置数”仅对应该冻结计数解，不表示所有可能约束/目标下的无条件全局最小替换数。新增上限从旧方案的 5 变为 7 是代价，不是经验证的质量阈值。

## 尚未通过的步骤

三 seed 增曝成员并集 40 张，其中 36 张属于前轮 56 张检查范围，另 4 张（基础池 3 张、常规 1 张）需要新的来源、完整标签和逐图内容审核。完整身份与具名缺口保存在 `completion.json/new_review_required`。前轮筛查记录不是全框质量准入证明。

“不增曝”仅避免进一步放大已知十二项风险，没有消除原有曝光中的监督风险，也没有证明优化有效。没有签发 ready；没有训练加载器预检或训练启动。

优先下一步仅为补审这四张新增增曝成员；通过后再冻结实际批次顺序并进行真实加载器核对，失败则保留具名缺口，不自动修改标签或降低门槛。

## 可复核性

独立审计重新计算成员计数、所有子集及全图类别实例曝光、增减量、风险上限和谱系曝光。重复 seed 不计作新增场景；登记谱系不直接等于独立场景。求解器复用独立临时目录中的 SciPy 1.18.1，不修改项目依赖。

完成入口记录相关 unittest 测试和 v2.11 固定 40 文件完整性核验，并绑定协议、审核、实现和报告哈希。不声明全仓测试通过。
