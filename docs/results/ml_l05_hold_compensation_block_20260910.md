# L05整图暂缓补偿：一次求解后的新质量阻断

## 已推进的工作

在独立l05-held-lighting-control-v1目录完成四张原条件图的全图与11个实例AI辅助审核，保留遮挡、截断和未见面板等限制。L02后方变压器上部主体和三个附件可辨、下部被柜体遮挡，与旧审核一致，并非新发现漏标或无遮挡批准。

随后在既有29张增曝候选范围内执行唯一一次三seed两阶段计数优化：先最小单图最大增量，再最小总绝对计数变化。L05及既有暂缓成员计数为零，原零曝光不恢复、负例计数不变、子集及完整标签类别实例总数相等、风险组上限不增加。三seed最大增量均为6，改动位置数分别为96、96、102；数值解通过独立整数约束核验。尚未生成实际序列，负例位置及亮度数组尚未经历新加载器验证，不能将计数可行描述成训练就绪。

18张成员需要增曝，其中4张已有本轮原条件审核；另14张按冻结顺序重放。12张技术检查通过，第13张I17发生语义冲突；I18未执行。技术通过的12张尚未完成本轮逐框质量批准，不自动全量通过。

## 新风险及证据

I17为candidate:c9baef248bd4fa3ddd62a38136f9ea63666e93b0951de96520ac121241d4506c。原标签只有电容器组178。稳定帧2、3、4均与原图RGB逐像素一致，实际位姿通过，相对RGB最大同步差为20、0、32毫秒。原178框坐标差0；重放新增transformer_se（实例57），每帧89,509个可见实例像素，可见区域半开框为[0,535,309,1080]。

已逐图查看全图、原尺寸ROI与掩码叠图。图左下边缘仅见严重截断的箱状侧面局部，依据实例映射及掩码确认归属，不凭颜色或局部外形猜类别。判断为“实例有可见证据，但内容不足”，组件身份未知。原全图标签没有覆盖此实例，构成监督风险。

新增full_2d几何框横跨几乎全宽，而实际实例像素只在左边局部；不能把整框面积当作可见像素，也不能自动以该框补写标签。投影框与可见区域差异的几何成因尚未在本轮单独认证。

I17在三seed旧序列分别曝光46、48、49次，本次解分别拟增至52、54、55次。补偿求解会放大此风险，因此该数值解禁止进入采样冻结与训练。未知项没有被排除统计。

## 停止与交付状态

遵守“一次求解后新增质量风险即停止”：未第二次求解、未采集光照变体、未冻结训练序列、未执行加载器预检、未训练。未修改历史图像、标签、权重、审核或协议。全部新产物training_admitted=false、promotable=false；不是ready_for_training_not_started。

相关47项回归及v2.11固定40文件完整性通过，具体输出与清理核验绑定completion.json；不声称全仓测试通过。

## 重要判断与下一优先方向

原计划目标清晰和旧标签框可辨，均不能代替全图实例完整性检查：L05与I17分别显示电抗器图及电容器组图中的非计划变压器遗漏风险。两例支持检查采集/标注边缘行为，不足以估计全池遗漏率，更不能确定模型退化唯一原因。

不应继续逐个删风险图并反复重求解。下一优先方向是独立的既定候选范围全图完整性核验：先汇总全部具名遗漏和严重截断风险，完成来源分组与统一处理规则，再申请并冻结新方案。本轮不自行扩大核验或训练授权。
