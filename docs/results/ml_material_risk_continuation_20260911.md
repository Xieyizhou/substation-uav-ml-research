# 材质可行性复核：风险隔离后续结果

## 判断

已将 T027、T036 的具名风险组登记为整图隔离、不恢复曝光；并继续完成四条件逐目标审核、六单元重计分及当前曝光核对。**这是风险受控，不是标签问题被修好了。** 本轮没有训练、补采、修订历史标签或解封新场景。

唯一优先建议：先做**已有权重对已曝光灰色训练成员的拟合诊断**，暂不直接启动下一轮材质训练。

原因：材质退化仍跨三个 seed 复现；训练实际已有相同灰色材质监督，但来源集中。现有证据尚不能区分“训练样本没学稳”和“学会了但未迁移到开发视角”。剩余 warm/cool 候选的干预范围也与四条件材质组不同。

## 风险处理与勘误

- T027 的三个原始/派生训练成员和 T036 的一个训练成员，在当前 R-clean、L-physical 六个单元中原本就全部为零曝光。旧候选风险不能直接推出“当前训练受到这四张图污染”。
- 新登记同时保留 T027/T036 warm、cool 暂缓，不删单个标签、不置空标签、不恢复同源成员。核验范围是已唯一解析的成员与谱系，不声称完成全仓同源闭包。
- 旧分层证据中的 96 条非计划目标 ID、裁剪引用问题已由独立证据替代；这不是修改原始真值。旧文件保留。
- 新指标实际读取并绑定本轮 `independent-review.json`，不再读取新版审核却绑定旧版。
- 四个物理光照派生成员的 `source_image_path` 仍指原始条件图。两图像素不同是登记的派生结果；本次改为与各自真实光照采集输出核对，像素一致。没有把光照变换称为无损转换。

## 审核与重计分

查看了 24 页不带预测的全图及逐真值裁剪，覆盖 48 张图的全部 240 个真值。每条记录具名场景实例、独立条件观察、理由、时间、原图/裁剪/审核页证据哈希；性质为 AI 辅助审核。

新分层：清晰 48、部分可辨 156、片段 24、无法确认 12。相同层级不是由原始条件自动传播：各条件均有单独观察。清晰表示主体内容可辨，不表示像素级可见性认证、类别无歧义或训练批准。

12 条 unknown 对应三个目标在四条件下的极窄遮挡片段：V06 entry_switchgear、V07 entry_switchgear、V08 transformer_mid。保留在全量指标中；若要解释这些片段自身的可辨内容，仍缺可靠的独立归属证据。

六个单元均复用哈希有效的正式/低阈值预测，并从原始配对真值重新执行一对一匹配。重计分与有效历史全量结果一致；1,440 条模型—目标事件完整，计划命中与一对一分配冲突为 0。它们仍只来自 12 个位姿组，不能算 1,440 个独立样本。

以下为三个 seed 均值，百分比：

| 条件 | R-clean 全图召回 | L-physical 全图召回 | R-clean 计划命中 | L-physical 计划命中 |
|---|---:|---:|---:|---:|
| 原始 | 73.89 | 72.78 | 86.11 | 83.33 |
| 材质 | 20.00 | 16.67 | 30.56 | 36.11 |
| 背景 | 64.44 | 62.22 | 77.78 | 69.44 |
| 光照 | 57.78 | 60.56 | 58.33 | 66.67 |

无目标 FPR：6.25% → 3.47%。逐 seed、逐类、匹配精度、未匹配预测、四类漏检及同实例得失见机器可复算账本。

固定原始清晰层包含每条件 12 个目标：R-clean 召回从原始 88.89% 降到材质 27.78%；L-physical 从 83.33% 降到 27.78%。这说明全量材质损失不能只由极窄、内容不足目标解释，但不能证明唯一机制是颜色。

其中清晰电抗器只有两个位姿：原始召回 R-clean 为 83.33%，L-physical 为 50.00%；材质两组均为 33.33%。必须单列，不能用其他类别或计划命中的改善掩盖它。小样本只支持本场景方向性判断。

## 实际曝光及候选覆盖

核验当前 240 个池成员的完整标签和导出来源 RGB；追溯到 416 个正例标签对应的保存世界材质。两个家族相同 seed 的灰色实例曝光一致：

| 类别 | seed 7 | seed 17 | seed 27 | 已曝光灰色来源位姿谱系数 |
|---|---:|---:|---:|---:|
| 电抗器 | 92 | 95 | 85 | 2 |
| 开关柜 | 245 | 258 | 230 | 2 |
| 电容器组 | 17 | 20 | 19 | 2 |
| 变压器 | 125 | 134 | 122 | 4 |

这里是按完整标签累计的**实例曝光**，不是图像数或独立场景数。同帧多个目标分别贡献监督。包装层 `variant=original` 并不能表示世界材质原始：它包含实际 neutral_bridge 和 background_bridge 来源，本次读取保存世界核对，未用包装字段代替语义检查。

65 个正例记录缺少原采集时的世界哈希/实例映射证明；可进行保存来源、像素、完整标签和世界映射的事后核对，不能倒称原采集已通过新门禁。120 张负例完成导出像素与空完整标签核对，未在本次目标材质统计中重新逐世界审核。全池新一轮漏标视觉筛查没有执行，不能声称所有监督风险已清除。

八张冻结候选逐项身份、完整标签计数、已有审核及原尺寸像素去重已核对：

- T020 warm/cool：保留为有界候选，只改变 capacitor_east 主体；来源成员三个 seed 各仅曝光 1 次。
- T023 warm/cool：保留为有界候选，只改变 switchgear_west 主体；来源成员曝光 61/68/60 次。
- T027、T036 warm/cool：继续整图暂缓，不恢复。

这四张保留候选不是四个独立场景，只来自两个同布局/资产位姿谱系。与开发图文件及原尺寸像素不重复，不等于来源独立；现有审核候选也不自动取得训练准入。warm/cool 仅改变选定主体，保留面板，不能等价覆盖开发材质组“各类主体及面板统一灰”的干预。特别是没有合格的新增电抗器主体材质变体。

## 下一阶段：先拟合核对，不直接增曝

冻结入口名单已写入完成回执：四个实际曝光的 neutral_bridge 成员（ID 前缀 2959b9、4aeb0f、830094、b9e46c），及其可解析的同位姿原始/背景训练成员；不恢复任何暂缓成员。

1. 对这些成员的全部标签核对现有明确逐框审核；缺项补审，内容 unknown 不当作有效监督。
2. 对当前六份终点权重及 v2.11 初始化进行同协议 CPU/640、0.37 与 0.001 推理。初始化不声称训练见过这些样本；每模型按实际曝光区分已见/未见。
3. 完整标签一对一匹配，逐实例比较训练侧与当前开发材质条件。沿用现有四类操作性漏检，保留竞争情况。
4. 训练侧有效样本仍错误：支持拟合不稳，再设计单变量优化/采样对照。训练侧正确而开发失败：支持条件迁移受限，再设计条件/视角多样性对照。身份或监督新冲突先处理数据。

不在本次回执内开始上述下一阶段推理，不启动训练；本轮可行性分析已经给出了它所需的具名入口。未设计临时材质替换配比，也未调整曝光预算来迎合结果。

## 验证与产物

针对性回归 34 项通过；v2.11 固定 40 文件完整性核验通过。未运行全仓测试，不声明全仓通过。既有基线验证器只核验其声明的固定身份范围，不把它扩展解释成保护集图像/标签字节全审计。

产物目录：`data/research/ml_training_recovery_v1/material-control-feasibility-v1`。

- `risk-containment.json`：具名隔离与六单元实际零曝光。
- `independent-review.json`、`review-pages.json`：240 条独立条件审核及可查看证据。
- `recomputed-metrics.json`：六单元完整重计分、分层、配对得失与漏检。
- `coverage-census.json`：240 成员、保存世界材质、实际曝光、八候选及具名缺口。
- `continuation-completion.json`：上述依赖身份、覆盖矩阵、验证及下一阶段冻结入口。

全部新产物 `training_admitted=false`、`promotable=false`。没有选择或晋升模型。**已证实材质条件下降和来源集中；尚未证实其唯一根因，更未证明增加材质曝光就能修复。**
