# 正样本重分配：质量门禁发现12项待核验

日期：2026-09-09。独立目录 `positive-redistribution-pretraining-v1`。状态：`quality_gate_blocked_no_sequence`。

## 已完成与暂停位置

三个seed增加曝光成员的并集为56张：基础22、常规26、桥接8。56张均重核原尺寸RGB与原来源像素完全一致、完整标签与原采集回执坐标对应；实际查看了全部56张的全图叠框，并对12项疑点查看原图坐标裁出的局部。保存56条AI辅助审核记录，不称盲审。

**12张图的非计划目标内容仍待确认，不能签发训练就绪。** 未冻结新训练序列、未运行新加载器预检、未训练。计数可行并没有失效，但不能等价为成员质量合格。

另外44张仅记录“本次全图筛查未新增重点标记”，不表示每个框均已认证，也不自动获得训练准入。基础池22张既有的实际模式/实例映射缺失继续保留；本轮没有据此补发历史门禁证明。

## 具体待核验目标

| 图 | 目标 | 局部已观察内容 |
|---|---|---|
| P05 | 电容器组 | 左图缘极窄带，主要是地面和设备碎片 |
| P09 | 变压器 | 只见浅色圆柱附件状部分，主体不可辨 |
| P17 | 变压器 | 右图缘窄侧身、底座及地面 |
| P31、P44、P47、P53 | 电容器组 | 框与前景变压器顶部、侧壁或附件重叠，后方目标内容待区分 |
| P33 | 电容器组 | 右图缘极窄碎片 |
| P40 | 开关柜 | 下图缘蓝色顶部平面，缺乏主体/面板信息 |
| P46、P54 | 变压器 | 主要是底座、地面及很少下身 |
| P56 | 变压器 | 右图缘极窄底座/地面片段 |

P编号属于本次56成员清单，不等同历史T编号。每项绑定member_id、完整label行身份、原采集annotation身份、坐标、图像/证据哈希及计划曝光。

原图与回执标签对应说明不是导出时坐标漂移，但不能证明监督内容足够；full_2d允许包含遮挡范围。没有实例掩码，不宣称目标绝无可见像素，也不证明标签类别或坐标必定错误。组件归属不清时保持unknown。

## 对计数方案的影响

12项待核验图在seed 7/17/27中，合计图像曝光由150/151/150变为206/209/209，即增加56/58/59次。这里不是独立图数，也不是单类实例曝光数。

它们都不在原四张电抗器风险图中。即便电抗器内容清楚，同图其他类别的片段监督也可能被一并加强；这正是不能只看计划目标或类别计数的原因。

## 下一步

先针对12个具名目标核验实例可见性/归属及已有同帧证据，不能直接把12张全判为应删除，也不能删框留图。证据不足时，可以在新的独立可行性检查中考虑“待核验成员不增加曝光”约束，但应明确它不同于整图暂缓；重新求解后才能改变已冻结计数，不自行覆盖。

当前停在质量门禁，不跳过到序列、加载器或训练。未改变标签、既有计数方案、阈值或模型。所有产物 `training_admitted=false`、`promotable=false`。
