# 配对标注校准结果（2026-09-06）

已完成 10 个独立仿真批次、62 次采集，形成 31 对 visible_2d / full_2d 对照：19 个暂缓目标视角，以及三张地图共 12 个柜体/开关柜控制视角。全部采集回执为 complete_pending_review，未进入训练。

31 对 RGB 均逐像素一致，相机位姿均满足采集容差；配对世界 XML 除 box_type 外一致。因而本次两种框的差异可以归因于标注模式，而非画面变化。

| 比较范围 | 匹配目标数 | IoU 中位数 | 最低 IoU | IoU < 0.9 |
| --- | ---: | ---: | ---: | ---: |
| 全部配对 | 77 | 0.984191 | 0.006196 | 17 |
| 19 个暂缓目标视角 | 46 | 0.985977 | 0.412619 | 9 |

暂缓视角中的 46 个目标均有至少一个框端点相差超过 1 像素。另有 2 个控制视角的实例清单在模式间不同。IoU 数值用于描述差异，不作为事后制定的准入阈值。可见范围框不能通过端点加 1 转换成完整范围框，也不能仅凭多数框接近就批准混用。

已由助手查看全部 8 页、31 对开发集叠框图，每幅画面缩至 640×360。第 1 页可见遮挡目标的范围差异；第 5—8 页显示普通柜体与开关柜外观接近，但源场景类别不同，普通柜体仍未赋四类目标标签。这是视觉诊断记录，不是独立人工逐像素标注认证，也没有测量模型误报率。控制视角匹配距离、高度和方位，物体几何、相机俯仰及背景并不完全相同。

保持现有 426 帧候选清单；19 帧标注范围暂缓、23 帧不完整采集暂缓仍分别保留。此次未改历史标签、未将新采集纳入训练、未启动训练或替换模型。新配对结果不能证明历史数据当时使用了 full_2d，也未证明新采图与历史原图逐像素一致，因此不得直接向历史图移植新框。

下一步先追溯旧训练/评估数据的标注配置与原始真值，冻结统一的标注范围规范；对无法证明兼容的数据隔离或按明确规范重采。再在这些开发控制视角上运行冻结基线，量化普通柜体误报与开关柜漏检，决定是否需改善场景的类别可辨识性。补足类别覆盖、完成数据准入后再做受控训练。

证据位于 `data/research/ml_training_recovery_v1/paired-calibration-v1/`：plan-manifest.json、各批次 capture/collection-receipt.json，以及 analysis/report.json、pairs.jsonl、object-comparisons.jsonl。报告绑定原始输入、图像/深度、结果和 8 页叠框图哈希；报告 identity 为 `943a148a8ed1580b45362217965a9224e5c18559cc33b2cbfd47eb883e1b8395`。叠框图位于 `outputs/research/ml_training_recovery_v1/paired-calibration/`。

冻结基线验证通过：40 个固定文件和模型包完整性正常，training_data_ready 仍为 false。

验证：报告全部输入、结果、叠框图哈希及 identity 复核通过；使用 unittest 运行配对计算与可见框适配测试，9 项通过。环境未安装 pytest，使用项目现有 unittest 方式完成验证。git diff --check 无错误，采集后未发现遗留 gz sim 进程。
