# 四类新位姿材质三联组：采集与审核

完成8张暖／冷变体，连同上一阶段4张原始图，共12张候选、4个完整三联位姿组。每类1个位姿，不是原计划的12位姿/36张全量材料。没有训练，没有修改历史标签，没有恢复N/P暂缓组。

## 材质和技术检查

暖色ambient/diffuse为 `0.55 0.48 0.36 1`，冷色为 `0.25 0.28 0.32 1`，四类跨类别复用。只改变计划设备主体的两项颜色字段：G01 capacitor_east，G02 west_switchgear_01，G03 reactor_north，G04 transformer_mid。前面板、基座、附属结构、其他设备、几何、相机与光照不变。电抗器使用reactor视觉体，其余使用body视觉体。

八张均第一次技术尝试成功，保存三帧稳定RGB、深度、实例掩码、实际位姿与框输出。每个变体三帧掩码都与对应已认证原始来源逐字节一致，完整框成员不变、坐标差异≤1像素；再次核对全图没有可见映射目标缺框。RGB确有变化，不把变体称为原帧RGB完全一致。

## 逐图审核

新生成全图叠框和各自独立裁剪，逐条记录24条AI辅助审核决定，绑定图像、裁剪、完整标签和采集身份；未沿用原始组决定自动批准变体。

- G01：两种颜色下电容器封闭主体、顶面、基座可辨，非计划变压器主体与三套管清晰。
- G02：计划柜体暖／冷主体可辨；部分后排柜体仍有邻物遮挡，但保留宽主体、面板或顶面。其他五个同类柜体没有一起换色，不把全图同类框数称为材质变体实例数。
- G03：暖／冷电抗器圆柱、顶面和基座完整，冷色较暗但轮廓可辨。
- G04：暖／冷变压器宽主体、套管和基座清晰；后方电抗器右下部受遮挡，明确保留部分遮挡状态。

本批没有发现新增整图质量阻断。审核通过只针对此次有界候选内容，不等于正式训练准入或跨场景有效性。

## 身份与下一阶段

完成回执中的12项成员清单保存pair_id、variant、全图标签、实例映射、实际位姿及图像哈希。三联组为同一来源关系，不能拆成独立样本膨胀数量。仍共享complex布局和资产。

下一阶段先核验这12张的文件/原尺寸像素去重、数据角色隔离与完整标签导出，再按已冻结的几何预筛策略补足剩余8个位姿，逐来源验证后才生成变体。最终是否能保持固定类别曝光和负例位置，仍须独立求解与真实加载器预检，不因本小批成功提前宣布可训练。

历史旧审核依赖链失效问题保持记录，当前阶段没有重新认证它。所有产物training_admitted=false、promotable=false。35项针对性测试及v2.11固定40文件核验通过，不声明全仓测试通过；没有模型推理择图或模型改进结论。
