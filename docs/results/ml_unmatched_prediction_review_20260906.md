# 小样本拟合后的未匹配框复核

对 batch-matched 诊断权重在同一 18 帧训练图上的 61 个预测框重新匹配后，42 个框以同类别、IoU≥0.5 命中 44 个真值中的 42 个；19 个框未匹配，另有 2 个真值实例没有被命中。随后查看 4 页叠框图，将 19 个未匹配框逐一归因。

| 归因 | 数量 | 说明 |
| --- | ---: | --- |
| 确认类别错误但覆盖真实目标 | 4 | 编号 1、3、15、18；预测框与目标重叠，但类别错误 |
| 确认背景幻检 | 15 | 其余编号；预测框位于空天空景，不覆盖任何真值 |
| 未匹配真值实例 | 2 | 在当前置信度 0.37、IoU 0.5 的匹配协议下没有对应预测 |

这 15 个背景框大多是天空中的 transformer 预测，说明小集拟合后仍保留背景混淆；4 个类别错误主要表现为 reactor/capacitor_bank 被报作 transformer 或 switchgear。它们属于训练内行为，不代表正式数据上的总体误报率。视觉归因由助手完成，已经查看全部四页叠框图，但不是盲审或独立人工标注。

此次结果支持两个判断：训练权重确实更新并提升了小集记忆，但提升伴随背景幻检和类别混淆，问题不能只用“学习率过低”解释；后续需要针对背景外观和类别可辨识性补充来源明确的难负样本与易混类别，同时保持正式评估隔离。不要把这 18 帧或 19 个框直接加入正式训练配额，也不要用训练内结果选择部署阈值。

下一步可以建立一份只含已审核负样本的开发对照，比较加入前后的背景幻检变化，并在独立开发视角上复验；正式准入仍需通过标注范围、来源、去重、覆盖和保护集门禁。当前 426 帧候选、19 帧范围暂缓、23 帧不完整采集暂缓和冻结 v2.11 均保持不变。

证据：data/research/ml_training_recovery_v1/memorization-v1/unmatched-audit-v1/final-review.json 及 report.json；叠框图位于 outputs/research/ml_training_recovery_v1/unmatched-audit-v1/。报告绑定原始输入、权重和叠框图哈希，final-review identity 已生成。

已将 15 个背景幻检框生成独立裁剪，作为待审核难负样本候选：data/research/ml_training_recovery_v1/background-hard-negative-candidates-v1/manifest.json。裁剪仍绑定原始整帧真值和源图哈希，明确标记 requires_review_before_training=true；因为源帧含有其他正样本，不能把整帧当作负样本。清单 identity 为 `bab2c58ffef3b40b316fa68111036db5f425554cfb26772455465f7c2351d4b9`，15 个裁剪哈希已复核。下一步进行人工语义确认，确认后才考虑进入独立开发训练对照。

人工语义复核已完成：14 个裁剪确认不含四类目标，可作为区域级难负样本候选；编号 11 只露出疑似设备部件，保持 hold_component_ambiguity。复核不是盲审，也没有改变任何原始标签。semantic-review.json 的 identity 为 `88105f049031c925549dd41c238d8ecd7e68a2bada900ee623fcaff1bc510790`，所有输入与裁剪哈希已复核。14 个候选仍需单独训练门禁，尚未加入正式训练集。
