# 119 帧相似性复核与当前批次处理

后续状态：参考池补漏重新筛查已将本文的 431 帧候选更新为 419 帧，8,317 帧回放候选更新为 8,301 帧。本文保留当时审核记录，最新结果见 [参考池与分组审计](ml_reference_group_audit_20260905.md)。

已逐张查看 119 帧开发侧缩略图（每张 480×270，10 页），并对 5 个仅命中开发回放的示例对进行 640×360 并排检查。所有处理均记录图像 SHA256、原像素核验结论、原近邻记录、图页位置和视觉观察。

| 样本 | 观察与依据 | 本轮处理 |
|---|---|---|
| 6 帧目标图 | 同属 medium-7614 录制，设备、地砖和杆体构图相近；均命中 qualification 指纹 | 按原近重复排除规则不纳入 |
| 108 帧背景图 | 主要为天空、围墙、地面，部分有彩色地面标记或边缘细节；命中保护指纹 | 按原规则不纳入 |
| 5 帧背景图 | 与记录中的开发回放示例在围墙、地面和天空分布上明显不同 | 记录“示例构图不同”，但本轮不放宽 Hamming≤2 规则，仍不纳入 |

这是当前批次的保守处理决定，不是将 119 帧都确认为真实近重复。保护图像未进行视觉查看，仍只使用已计算的排除指纹。缩略图检查也不替代完整标签重审。现有图像和标签保留，当前候选池保持 431 帧，其中目标 270、无目标 161；完整逐帧记录与保留候选清单位于 `data/research/ml_training_recovery_v1/near-review-final-v1`。

在本轮处理上，119 帧已没有悬而未决的“是否加入当前池”项；其真实近重复关系并未全部证实。回放侧另有 7,258 帧不在本轮视觉审核范围内。

## 后续工作的可执行路径

回放侧 7,258 个命中中，6,485 是空标签帧，773 是目标帧。剩余 8,317 个未命中项含 6,812 个目标帧和 1,505 个空标签帧，覆盖 30 个 recording。使用项目现有 `_select_replay` 算法进行了可行性检查：能够选出每类配额 1,000、共 4,000 个不同 sample_id 的回放样本。因此可以先基于未命中回放池推进后续完整性检查，不必把逐张审核全部 7,258 帧设为启动任何后续工作的前置条件。

此检查不表示训练准入：历史参考完整性、完整录制/视角隔离、正式困难样本配额仍待处理。困难样本当前覆盖为 transformer 214、switchgear 194、capacitor_bank 209、reactor 60、无目标 161，多目标帧可计入多个类别；不能拿回放样本替代该配额。下一阶段应优先完成可用候选的分组与参考完整性核对，并审核已有 canonical 增量后重算缺口。

证据：`report.json`、`review-decisions.jsonl`、`candidate-manifest.json`、`replay-selection-feasibility.json`。视觉图页在 `outputs/research/ml_training_recovery_v1/near-review`。所有图像与输出摘要已核验；保留 431 帧标签对象与原始 manifest 完全一致。

复核命令：`.venv/bin/python scripts/vision/finalize_near_similarity_review.py`。该脚本记录本次已完成的视觉审核，不能用于给任意新批次自动审核。
