# T027 剩余同源帧：独立重放核验

original 和 background_bridge 各自首次技术重放成功。使用已冻结的深度平面裁剪诊断构建；没有安装生产库、修改原始世界或训练标签。

两帧均通过保存位姿、相机合法性、同步与连续三帧稳定核验，RGB 与各自原始图逐像素完全一致。最大同步差 32 毫秒；原有五个框均保留，坐标最大差零。每帧只新增实例 128 的 full_2d 框 `[0,1051.845703125,348.4080505371094,1080]`。

分别读取两帧自己的实例掩码，目标像素数均为 3534，可见半开包围框均为 `[59,1052,306,1080]`。不是从 neutral 复制证据。结合此前 neutral 核验，当前已知三个训练变体均有同帧证据支持实例 128 可见而旧 full_2d 漏框。

本轮查看两帧原始局部及独立实例叠图：底图缘只有极窄的青色斜面片段，设备其余主体被图缘截去，无法从该片段可靠辨识完整设备内容。AI 辅助审核结论为“实例有可见证据，但内容不足”，整帧继续暂缓，不认证训练适用性。像素级边界的一像素归属歧义未借此宣告解决。

重要判断：实例归属与漏框证据已补齐；剩余问题不再只是技术上能否补框，而是极端截断的监督策略。建议下一阶段制定独立、整帧及同源组一致的处理清单，并核算实际训练曝光影响。T036 的遮挡内容风险仍需保留。不得直接补一个框后恢复训练，不删除单标签或自动置空，不改变历史实验身份。

本次只完成两帧的技术证据和风险局部审核，不是全池标签修订完成，也不是模型效果改善证据。训练、生产部署及新场景评估均未启动。
