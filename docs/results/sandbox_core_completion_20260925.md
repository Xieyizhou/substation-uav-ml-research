# 桌面沙盒闭环验收 · 2026-09-25

三个约定目标已完成：核心验收可独立运行、桌面模型迭代闭环实际走通、当前运行与冻结历史证据分离。
范围是现有固定场景中的开发沙盒；不包含手机布局或真机资格，也不承诺每次视觉任务都成功。

## 核心与源码交付

| 检查 | 结果 |
| --- | --- |
| 全量回归 | 2,093 项通过 |
| 独立源码、新核心依赖环境 | 974 项通过，173 个模块，无跳过 |
| 研究复现隔离执行 | 261 个模块全部通过 |
| 桌面 Chrome，1440 × 1000 | 页面身份、内容、交互、无错误遮罩、控制台与截图核对通过 |
| 连续桌面加载 | 4 次，无请求失败、无脚本异常 |

核心与研究依赖、测试入口分别声明，CI 运行核心集合。历史实验保留冻结实现，当前飞行使用显式传入
项目、PX4、场景、模型和输出目录的核心入口，避免实验脚本之间修改全局目录。
源码导出不含虚拟环境、模型权重或研究数据；约 6.6 MiB ZIP、13.4 MiB 文件内容。
这不是整个本地研究目录的磁盘占用，也没有删除原数据来制造体积下降。

## 已执行的桌面闭环

1. 真实飞行采集 51 张反馈，在浏览器中完成查看和显式审核：46 张接纳、5 张拒绝。
   审核者记录为 `Codex visual review (AI)`；原预测未自动变成标签。
2. 注册 `reviewed-flight-20260925`：302 张训练（256 张基础回放 + 46 张接纳反馈）、64 张基础验证。
   反馈整组只进入训练；已注册快照不随后续审核变化。训练实际保留全部 46 张接纳反馈。
3. 从 `workbench-full-v2-20260817` 微调得到 `reviewed-flight-ft-20260925`。
   CPU / 640 px / batch 4 / 5 epochs，训练约 282 秒，阶段峰值进程内存约 1.93 GB。
4. 比较使用实际父权重和相同验证成员。父 Macro-F1 0.970238，新模型 0.968013，下降约 0.22 个百分点。
   两者小目标召回均为 0.6，但只有 5 个小目标。这是开发验证，不代表独立泛化或质量提升。
5. 在 Flight Console 选择新模型，完成当前代码的正向验收、浏览器中途停车降落，以及验收后的实际任务。
6. 最后任务新增 53 张反馈，全部显示为待审核，注册按钮保持禁用，供下一轮显式审核。

| 当前运行用途 | 受管任务 | 运行目录 ID |
| --- | --- | --- |
| 正向验收 | `20260925T034321Z-semantic-flight-4668104a` | `semantic-a6965bcdc2e6445799388d1e70c0f95a` |
| 中途停止验收 | `20260925T034518Z-semantic-flight-2e83e0a6` | `semantic-2f8b945ce4ce4345a0b13e14c5d712bb` |
| 已验收任务 | `20260925T034924Z-semantic-flight-27dd1818` | `semantic-49db49d1b99b4bb288bb0b99473808d2` |

当前身份：`ada379b8f9c211629b52ab0fe8ed74dae9f78b541a6941d9d9bcf087378fb631`。
上述进程退出码均为 0。停止任务在通用任务表中以 `user_cancelled` 结束，独立停止回执确认验收通过；
不将被取消的巡检描述为完整巡检成功。

最后任务使用的 ONNX 为 `b8f4a676ebb88b3bf79d16e816098c927611c2e674eefa8b2398118261b3d878`。
它完成三次同步视觉确认、停车、换路线、恢复运动、到达及降落。到达误差 0.023804 m，
最大横向误差 0.033994 m，最小 LiDAR 距离 6.7561 m，全程碰撞球间隙 2.7248 m；
确认落地、解除武装、进程与端口释放。

## 失败记录与边界

第一次已验收任务 `semantic-5c89effc28a4444295139295737c82bc` 因规划后的视觉复核未通过而中止，
正常受控降落、解除武装并清理。随后同一代码、同一模型、同一保护阈值的复跑完成任务。
这次失败保留在任务历史中，不计为成功，也不据此宣称固定场景的成功率已经统计验证。

实际连跑还发现并修复两项基础问题：HTTP 默认待接收队列不足导致并发刷新连接重置；
TCP TIME_WAIT 被误判为活跃端口占用。后者现在允许正常 TCP 地址复用，仍拒绝活动监听器和被占用 UDP。
修复改变运行身份，修复前的结果没有被沿用为新实现的资格。

## 历史与当前运行的隔离

历史包清单：`c6e64ea91907d7214382321114698c65e4c8737a45303858906345a85714d66c`。
重新核验 50 份记录、14,066 个去重对象；6 次 LiDAR 正向、1 次无路拒绝、1 次视觉正向和
1 次受控停止的历史结果仍可独立验证。原记录字节与哈希保持不变。

当前入口仅从证据包恢复六个固定场景资源；当前源码、模型、PX4 资源与依赖版本分别绑定。
搬迁检查在禁止打开原项目和原 PX4 路径的 Python 审计钩子下通过：相同身份、场景首次恢复、
复制后的真实正向/停止记录核验、已验收任务协议准备均成功。修改证据被拒绝，修改源码使旧资格失效。
搬迁目录只做了准备与资格检查，没有声称在那里实际起飞。

本机详细报告和截图：`outputs/sandbox/verification/desktop-core-20260925/`。
正式飞行证据：`outputs/sandbox/semantic_runs/`；数据集与训练回执：`outputs/sandbox/workbench/`。
这些大体积本机产物不随源码包进入 Git。
