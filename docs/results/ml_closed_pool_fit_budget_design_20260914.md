# 已有权重拟合核对及训练预算对照

六份 E/M 终点权重对冻结 333 张池成员完成正式 0.37 与诊断 0.001 推理，合计 3996 次预测调用，不含预热。CPU640、同类NMS0.7、max_det300、同类一对一IoU≥0.5；两个并发进程，初始化后核验每进程4线程。无训练、优化器、反向传播或训练验证。

每个模型按自己的实际2700曝光识别训练见过与未曝光成员。E每seed235张、M257张，跨六单元共同曝光235张；不能直接用不同成员分母比较训练能力，也不把变体/seed当独立来源。

## 事实与解释

M三个seed新增来源完整标签召回：开关柜均13/22；电容器分别4/8、4/8、6/8；电抗器6/8、8/8、5/8；变压器7/10、6/10、8/10。这是实际曝光成员，说明并非训练侧全部学稳、仅剩开发迁移。冷色电容器三个seed均未命中。

共同235成员的电抗器，E为34/34、33/34、33/34，M均34/34；但开发电抗器仍有损失。训练拟合与开发表现可分离，多个机制可能并存。完整标签统计不是逐框清晰子集审核，不能把所有训练漏检自动解释为优化问题。

优先检验“增加完整训练预算”的效果，不宣称曝光不足为唯一原因。新目录 repeated-budget-control-v1 已冻结设计：B450、B900各seed7/17/27，六个独立v2.11初始化；900步完整重复原M的2700成员和亮度系数，前450步一致。其他增强、batch/nbs6、CPU640、AdamW、LR0.0005固定。原始、负例、完整类别曝光均随整段重复增加，不是纯材质增曝实验。不得按表现追加预算或挑seed。

两组重新训练，使用同一经过独立测速的CPU策略，以免历史训练实际线程数未验证成为预算对照的混杂因素。仅用预定终点last.pt；现有开发评估、门槛、风险/暂缓成员状态不变。不补采、不修改标签、不解封测试，不晋升。

## 当前状态

设计及可变预算加载逻辑已建立，尚未启动训练。下一步为真实训练短程CPU测速、六配置完整加载预检与就绪核验，再启动完整训练。推理加速结论不直接用于训练。训练预算不能绕过质量/身份门禁。

拟合分母、一对一匹配、零分母、完整重复、亮度及额外参数漂移等5项针对性测试通过；不声明全仓测试通过。所有产物training_admitted=false、promotable=false。
