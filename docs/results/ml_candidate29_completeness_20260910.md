# 既定29张候选：全图实例完整性统一核验

## 结论

29张候选均有来源核验、重放结果与显式全图AI辅助筛查记录。18张复用有效证据，11张补做原条件诊断重放。检查没有扩大到训练池全部236张，也没有访问保护标签或封存测试集。

| 结果 | 数量 | 含义 |
| --- | ---: | --- |
| 未检出实例成员冲突 | 26 | 映射实例、原标签框与对齐重放输出一致；不等于所有标签内容充足或训练批准 |
| 确认遗漏风险 | 2 | A25=L05、A28=I17，均为图缘非计划变压器片段 |
| 严格边界数值异常 | 1 | A16，原始与重放同样存在极小越界，保留具名缺口 |

前两例并非本轮新增加的遗漏；本轮补齐范围后，未检出第三例同类实例成员遗漏。不能据此报告随机抽样遗漏率或断言全池无其他风险。

## 两张遗漏与一张技术异常分开处理

A25/L05：历史标签只有电抗器208，右图缘另有transformer_mid（64）侧面及底部局部。A28/I17：历史标签只有电容器组178，左下缘另有transformer_se（57）侧面局部。两者原帧与重放RGB完全一致、原有框不漂移，实例掩码提供稳定可见证据；局部内容不足，不自动补写几何框或设置ignore。

A16为base:fb7c8bf0495a92d55c7b074842fd84237f5f18d2470fb2095d04628e0de5612e，来自medium场景。原始与重放四个几何框完全一致，RGB完全一致，未见无框的映射实例像素。电容器组178框的下界为1080.0000305175781，比图高1080多0.000030517578125像素。三个稳定帧均如此，实际位姿与同步核对通过。这与浮点表示误差相符，不能称第三张确认漏标图；严格门禁仍保持失败。本轮未裁剪、重写原框或放宽检查。

## 全图筛查的覆盖与限制

29张全图已逐一查看并记录理由。近景边缘截断、杆体遮挡、后方设备仅上部可见、未见前面板等限制逐帧保留。存在的桥接同源变体不算新独立场景。视觉像箱体的背景结构不能凭外观改为目标设备身份。

本轮对28帧生成84个实例证据裁剪（其中2个为新增遗漏实例），A16另独立保存四框原始数值核对。证据生成不是逐框批准；此次明确完成的是全图完整性筛查，而非84个实例的全量质量批准。遮挡严重、内容不足等既有监督风险未因成员一致而自动解除。组件级身份没有可靠ID时仍为unknown。

## 统一处理建议

建议以一个独立修订方案统一处理，而不是继续逐图剔除并重求解：

1. A25与A28列为整图暂缓建议，追踪已确认同帧派生关系；原图、原标签和历史训练不改写。原有其他暂缓及零曝光约束保留。
2. A16列为技术异常，不直接永久剔除。下一步先做独立数值边界回归，验证机器精度误差和真实越界的区别；任何容差必须有数值依据、保留原坐标与修正日志，并重新核验后才考虑放行。
3. 对实际拟增曝成员复核完整逐框质量决定，不能以此次全图筛查代替批准。风险范围统一冻结后，才能制定新的有界配额方案及同数据参考／光照对照。

当前不签发训练就绪，不执行第二次求解、不生成新训练池、不训练。所有新产物training_admitted=false、promotable=false。

## 验证

本轮相关53项回归及v2.11固定40文件完整性核验通过，进程清理已核验。具体输出与输入哈希绑定completion.json；不声称全仓测试通过。此次终点是29张有明确核验结论或具名缺口，不是所有监督问题已解决。
