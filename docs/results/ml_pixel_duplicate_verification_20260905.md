# 跨编码像素重复核验

核验已完成。对本次提供的保护索引、550 帧已审核候选、修正后的 15,575 帧开发回放样本，未发现新增的完整像素重复。先前移除的 10 条开发侧文件重复未重新加入。

处理 138,723 个不同文件 SHA256，每个哈希选择一个实际文件复算文件摘要，再解码为原始尺寸的 RGB8。像素摘要绑定宽高、算法版本与逐行完整 RGB 字节；不缩放、不裁剪、不重新压缩。保护索引共 130,154 条成员记录，保护图像仅用于排除指纹，不读取标签或运行模型评估。

| 检查对象 | 数量 | 新增像素完全重复 | 仍待相似性复核 |
|---|---:|---:|---:|
| 已审核候选 | 550 | 0 | 119 |
| 已修正开发回放池 | 15,575 | 0 | 7,258 |

候选中 431 帧既未命中上一轮近重复规则，也未发现本轮像素完全重复。回放池对应数量为 8,317 帧。其余项只能确认像素不同，不能据此宣布为近重复误报：微小运动、曝光变化或邻帧仍可能具有近重复关系。原相似性隔离状态继续保留，派生回放清单未接入训练入口，训练准入仍为 false。

补充检查仅针对开发候选：119 帧相似命中均非纯色 RGB 图，每帧至少一个通道的标准差不低于 5（8 bit）。因此没有证据将这些命中统一解释为空白或纯色背景。此统计不判定图像语义，也不能否定低纹理局部区域的影响。

后续应优先复核 6 帧目标候选及 113 帧背景候选的相似关系，按录制/视角组归并回放侧命中。不能把像素不等同直接用作放行依据，也不应只因 dHash 相近删除 7,258 帧。其余历史 hard pool、完整 qualification 发现范围和视角谱系仍有先前记录的边界。

产物位于 `data/research/ml_training_recovery_v1/pixel-dedup-v1`：

- `report.json`：输入、输出、算法和状态摘要。
- `pixel-fingerprints.jsonl`：138,723 个文件及完整像素摘要。
- `reviewed-decisions.jsonl`：550 帧候选逐帧结论。
- `replay-decisions.jsonl`：15,575 帧回放逐帧结论。
- `replay-pixel-distinct-membership.jsonl`：保持 15,575 帧，仅通过像素完全重复这一项检查的成员清单。
- `development-similarity-texture.json`：119 帧开发候选的 RGB 通道统计。

验证：4 项单元测试通过，涵盖 PNG/PPM 同像素、单像素变化、宽高绑定及拒绝静默移除 alpha；全部输入输出摘要、行数和报告身份核验通过。同一文件 SHA256 的别名路径并未逐个复算；统计为不同文件哈希代表的核验数量，不是所有路径的完整性核验。

复核命令：`.venv/bin/python scripts/vision/verify_pixel_duplicates.py`。
