# Gamma 对照：正式开发评估结果

## 判断

当前冻结 Gamma 策略未改善预定材质与光照指标，未通过门槛，不选择候选、不晋升、不自动追加训练。背景全图召回有收益，但不足以抵消材质、光照和误检退化。此结论只针对本配方和已查看开发集，不意味着所有 Gamma 或增强方法均无效。

三个 G900 seed7/17/27 均完成900步，终点权重及实际曝光核验通过。与同seed B900保持成员、完整监督、亮度序列和预算不变，仅新增冻结Gamma。实际训练曝光账本重新比较一致；恒等Gamma批次张量一致。没有修改历史实验。

## 统一结果

48张配对开发图（12个位姿组）、48张无目标开发图；CPU640，正式confidence0.37、诊断0.001，同类NMS IoU0.7，max_det300，同类一对一匹配IoU≥0.5。双进程每进程4线程，三个新模型共576次计分推理调用，另有预热；运行器约51.5秒，历史B900身份有效的预测复用。

三seed均值，单位为百分比。

| 指标 | B900 | G900 | 变化（百分点） |
|---|---:|---:|---:|
| 原始计划命中 | 97.22 | 86.11 | −11.11 |
| 材质计划命中 | 47.22 | 38.89 | −8.33 |
| 背景计划命中 | 83.33 | 80.56 | −2.78 |
| 光照计划命中 | 58.33 | 44.44 | −13.89 |
| 原始全图召回 | 74.44 | 73.89 | −0.56 |
| 材质全图召回 | 25.56 | 23.33 | −2.22 |
| 背景全图召回 | 61.67 | 66.67 | +5.00 |
| 光照全图召回 | 52.78 | 46.11 | −6.67 |
| 无目标帧FPR | 1.39 | 5.56 | +4.17 |

G900计划命中逐seed7/17/27：原始91.67/66.67/100%，材质33.33/33.33/50%，背景75/66.67/100%，光照50/25/58.33%。材质与光照均是两个seed下降、一个持平。负例误检帧数2/4/2，对照为1/1/0，三个seed均增多；新组仍满足既定FPR上限，不能把“比对照变差”误称为FPR绝对门槛失败。

原始电抗器均值73.33%保持不变，但光照电抗器33.33%→13.33%，下降20个百分点。原始变压器下降7.41个百分点，光照变压器下降12.96个百分点、光照电容器下降5.56个百分点。原始全图平均接近持平并不等于各类能力保留。

G900失败项包括原始最差seed、材质均值、光照均值及固定参考能力保留。门禁仍相对retained_reference-450和历史A；历史函数字段same_budget_R并非新900步参考。所有逐类、逐seed精度、召回、未匹配预测及政策检查均保留在summary.json和逐模型JSON，不调整阈值。

## 配对与审核状态

重新按完整真值复算一对一评分及负例指标，均与保存结果一致；计划命中/全图分配冲突0。720个实例-seed转换为315持续命中、271持续漏检、63得益、71损失。重复seed不是新增独立样本。

71损失事件对应61个条件内目标；操作性分类50同类低置信度、18错类、3未发现合格保留预测。4个事件涉及正式匹配竞争，仍需逐框解释，不将这些分类直接等同网络机制。低阈值受NMS/max_det限制，最后一类不是“网络没有产生候选”。

已生成68张错误证据卡（17页）：新组8个无目标预测、7张唯一图像，以及61个损失目标。身份从原映射解析，每个目标有自己的裁剪；得益、持续命中及持续漏检全部保留。**这些是待审核证据，不是已审核决定。** 本次没有自动生成批准或替用历史审核，也没有签发审核完成回执。

## 下一步

先审核这8个误检及61个损失目标，解释4个匹配竞争；核查损失是否集中于清晰主体、遮挡边界或某种明暗条件，再决定是否需要现有权重的训练拟合对照。当前不支持直接保留该Gamma策略继续加量，也不能只凭本次结果认定颜色依赖、量化损失或增强过强为唯一病因。

15项针对性回归通过，数值审计独立复算通过；本次summary绑定v2.11固定40文件完整性核验。不声明全仓测试通过。全部产物training_admitted=false、promotable=false，新场景仍封存。

证据目录为 `repeated-budget-control-v1/gamma-transfer-control-v1/evaluation`：`G900-{7,17,27}.json`、`summary.json`、`numerical-audit.json`、`error-review-v1/evidence.json`。状态为数值评估完成、显式错误审核待办。
