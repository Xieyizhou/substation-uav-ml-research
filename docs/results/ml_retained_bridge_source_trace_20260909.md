# 保留桥接成员来源核验

## 判断

39/39 张保留训练成员、13 个登记谱系的来源核验通过，可以进入小规模实例重放验证；尚不能认定全部实例内容可辨识，也没有新的合格模型。

核验回执身份：`4d6d3a287fa54ff79db4df4f543a7519543570c85ad55f366e2193ca6f79805e`。产物位于上一阶段 `supervision-preservation-feasibility-v1/bridge-source-trace-v1/trace.json`。

## 已证实

- 每组包含 original、neutral_bridge、background_bridge 三种来源变体，共 39 张，不视为 39 个独立场景。
- 训练 PNG 与原始 RGB 解码像素一致；159 个完整实例框按原始导出规则逐字对应训练标签，没有在本次导出环节丢失全图监督。
- 回执、世界、传感器配置、实例映射及保存位姿可追溯；当前配置语义、实例存在、相机合法性、位姿误差和保存时间戳同步检查通过。
- 计划目标是 7 个电容器组位姿和 6 个电抗器位姿；这不是专为开关柜设计的 13 个独立位姿。全图包含其他类别，不能把计划类别当作完整监督分布。
- v2.11 固定 40 文件完整性通过。相关 28 项单元回归通过，不声称全仓测试通过。

## 风险与未证实事项

33 张图中共有 51 个框距图像边界不超过 1 像素。这里只是边界风险标记，不等于极端截断，也不自动拒收。旧审核理由主要围绕计划目标，不能由此认证其余实例。

保存的最终帧不能重新证明原始连续 3 帧稳定；这项目前仅有历史采集链路的接受规则，后续重放必须重新记录连续帧。未运行实例掩码重放，所有新可见性状态保持 unknown。

首轮脚本错误使用了历史计划不存在的 views 字段。失败输出及脚本快照保留在 failed-attempt-001；修正为读取 calibration_views/pilot_views 后，39 张重新核验通过。首轮失败是工具兼容问题，不是数据缺陷。

## 下一步

从来源通过的组中确定性选取小规模重放样本，优先覆盖“有开关柜共现且贴边”和“无贴边对照”，不依据模型得分挑图；保留每组全部变体，并逐实例查看主体、面板、基座和遮挡。原帧与重放 RGB 完全一致后才能将掩码用于原帧像素级认证。

先解决全图可见性证据，再讨论能否保留原桥接监督并设计训练对照。来源齐全不能消除上一阶段已证明的固定曝光约束冲突，不应立即继续堆训练步数。

本轮未采集、未修改标签、未训练、未解封测试集；training_admitted=false、promotable=false。
