# 交错曝光训练：当前错误审核

## 判断

I1000 三 seed 训练与正式/低阈值开发评估完成。本次新增 AI 辅助审核完成，保留一个具名内容缺口；不表示候选通过，不修订标签、不启动训练、不晋升。

交错曝光显著缓解了相同总曝光下的集中尾段退化，但不能据此声称材质或光照问题已经解决。相对 B900，原始全图召回从 74.44% 到 77.78%，光照从 52.78% 到 53.33%，材质从 25.56% 到 25.00%；材质计划命中提高不等于全图材质能力同步提高。

## 审核覆盖与发现

- 本轮全部 6 个无目标预测框，来自 6 张唯一图像：5 个柜状块体、1 个杆体与块体混合结构。这是视觉外形判断，不重命名仿真资产。
- seed 7 的 4 个框均预测为开关柜，置信度约 0.401–0.795；seed 17 无无目标误检；seed 27 的混合框预测为电抗器（0.928），图缘柜状块体预测为变压器（0.572）。重复条件或 seed 不增加独立来源数量。
- 相对 R1000 与 B900 两个对照，覆盖四条件全部新增漏检，跨对照/seed 合并为 58 个图像内目标：轮廓较完整 13、截断 33、遮挡 11、无法确认 1。
- E31：原始条件 capacitor_east，框内前景变压器柱体与后方青色块体重叠。仅凭此次 RGB 无法可靠分离目标内容，保留 unknown；不删除该标签、不剔除该指标。
- 无实例掩码，本次所有决定均不认证像素级可见性；“轮廓较完整”不等于充分质量准入。

## 配对事件

| 对照 | 得益 | 损失 | 持续命中 | 持续漏检 |
|---|---:|---:|---:|---:|
| R1000 → I1000 | 208 | 34 | 193 | 285 |
| B900 → I1000 | 66 | 51 | 335 | 268 |

以上为三 seed 四条件的预测事件，不是独立样本数。58 个审核目标与 85 次跨对照损失事件不可混称。

R1000 对照损失：18 同类低置信度、10 错类、6 未发现合格保留预测。B900 对照损失：30 同类低置信度、15 错类、5 定位不足、1 未发现合格保留预测。最后一类受 NMS/max_det 限制，不代表网络没有产生候选。所有得益、持续命中和持续漏检保存在 transitions 中。

## 门禁与下一优先方向

匹配冲突为 0。光照计划命中 58.33% 未到 60%；相对历史 A 的原始电容器组、电抗器召回及光照全图、开关柜召回仍有超容差退化。无目标 FPR 均值 4.17% 达标，但混合结构的高置信度误检仍值得诊断。审核没有改变任何数值门槛。

下一优先方向：先对剩余较完整目标及柜状/杆体混淆做当前权重的训练侧拟合与具名覆盖核对，优先定位光照开关柜与材质柜体；不要直接继续加步数。若有效已曝光样本也错误，再设计采样/优化单变量对照；若训练侧正确而开发失败，再设计条件覆盖对照。E31 保持单独的身份/可辨内容缺口，不能用于支持清晰目标退化结论。

审核记录位于当前 tail-interleaving-feasibility-v1/evaluation/error-review-v1，包括 evidence.json、review.json、review-binding.json、全图叠框与逐目标裁剪。所有产物 training_admitted=false、promotable=false；新场景保持封存。
