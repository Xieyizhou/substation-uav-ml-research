# 待定截断与遮挡框复核

2026-09-08，AI 辅助审核；不变更历史收录，不训练、不解封新场景。

已逐一查看冻结的 27 个待定框上下文裁剪，记录逐框中文理由及图像、标签、审核证据哈希。涉及 23 个图像成员、17 个登记谱系，不是 27 个独立场景。

| 问题分组 | 框事件数 | 处理 |
|---|---:|---|
| 已有深度支持的极端边缘框 | 3 | 保留既有证据，不能按空框删除 |
| 其他边缘片段 | 3 | 查实例投影及截断可见区域 |
| 重叠结构、面板归属不清 | 4 | 查具体实例掩码，不能借用前景面板 |
| 前景遮挡、后方仅露窄条 | 17 | 查后方实例实际可见像素 |

27 项全部保持待定；这是完成一次复核，不是解决全部可见性问题。裁剪中的前景顶面、立柱或箱体不能直接当作被标注开关柜的可见主体。其他 266 个已有条件记录未在本轮重新审核，不能称作新增质量准入通过。

重要判断：需要解决的不仅是三个边缘框，还包括重复出现的“框覆盖前景结构、目标只露窄条”的监督歧义。它是需要对照验证的标签质量风险，不是已证明的模型退化唯一病因。灰色条件下结构边界更难分辨，不能因此自动判作错误标签。

## 下一阶段的有界任务

先追溯这 23 个图像成员到原始采集、世界、实例映射及实际位姿。按 17 个登记谱系组织重放，但不能假设同谱系所有视图可互相替代；每个受影响帧都要验证对齐。用独立目录进行实例掩码重放，保存原图与重放图的差异，不覆盖原始证据。重放对齐失败仍待定。

确认实例可见区域后，依据统一规则决定派生版本的整图暂缓范围，并对完整池做谱系闭包。当前清单的闭包仅限开关柜审核 manifest，覆盖 23 个成员，尚非全池闭包。不得先删框、把图变为负例或直接启动清理后训练。

产物目录：switchgear-condition-review-v1/held-visibility-followup-v1。manifest.json 为证据清单，review.json 为逐项决定及建议暂缓清单；新产物均 training_admitted=false、promotable=false。
