# 边界数值验证、统一风险规则与质量预检

## 本轮完成的四步

1. 检查隔离渲染器的float32角点转宽高／中心实现，构造运算样例，复现1080.0000305175781下边界。该样例证明这种误差可由浮点计算产生，不等于恢复了A16未保存的每个网格中间值。
2. 新增仅用于诊断的边界归一化：在已核验1920×1080配置下，预算为相应图像尺寸处一个float32 ULP（两轴均0.0001220703125像素）。来源是有界角点的宽高减法及中心加法舍入误差，不是依据训练成绩选择的容差。真实越界、非有限坐标、重复或未知实例、非正面积、其他分辨率均不可凭此放行。保留原坐标、修正量和预算。
3. 用独立只读入口重新核对29张原始证据：A16在诊断归一化后没有实例成员冲突；A25/L05和A28/I17的遗漏仍然存在。原严格采集入口、旧失败回执和历史标签完全未修改，也未安装或部署新渲染器。
4. 冻结统一规则并重新校验既有逐框审核：原8张暂缓不恢复，新增两张风险图，未来新方案共10张零曝光；剩余27张均有哈希有效的既有逐框AI辅助审核。此次复用既有决定，不生成新的全量通过决定。

## 来源与范围

两张新增风险图分别在原候选登记中唯一匹配；对当前236张训练池比较原尺寸RGB后，没有找到需要额外列入暂缓的同像素成员。登记同位姿派生关系一起检查。此结论仅覆盖当前池及对应登记账本，不代表整个仓库来源谱系已闭合。今后发现确认同帧派生成员须同组处理。

新增暂缓成员：

- candidate:74a84f89ff66e645164692abb0d1e67fec055bb9081ee2188cc4b5b782d2849b（L05，图缘transformer_mid）。
- candidate:c9baef248bd4fa3ddd62a38136f9ea63666e93b0951de96520ac121241d4506c（I17，图缘transformer_se）。

“零曝光”是对未来独立版本的冻结规则，不是已执行删除或数据修订；原文件完整保留。A16不当作确认漏标图永久剔除，但诊断归一化尚未接入正式训练／采集准入。

## 当前判断点

此前反复阻断的两类问题已经分开：两张是真实可见实例未被旧标签覆盖的监督风险；A16是已有证据支持的数值边界问题。两者不能混为一类，也不能据此断言模型退化唯一原因。

当前质量与身份预检支持进入新方案设计，尚不是ready_for_training_not_started。没有重新求解配额、导出新训练池、冻结新序列、运行真实加载器或训练。历史内容审核中的遮挡、截断及同源集中限制仍保留。

下一阶段应独立冻结“统一暂缓后的原条件参考／物理光照处理”两组方案，重新核验计数、实际位置、完整标签、亮度系数和加载器一致性。不能把本次数值诊断成功理解为旧补偿解已经恢复有效，也不能绕过新方案冻结直接训练。

## 验证与身份

相关62项回归和v2.11固定40文件完整性通过；不声明全仓测试通过。所有新产物training_admitted=false、promotable=false。数据、协议、审核及脚本身份绑定在独立candidate29-unified-risk-policy-v1目录回执中。
