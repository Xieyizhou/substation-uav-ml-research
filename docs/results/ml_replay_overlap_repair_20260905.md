# 开发回放池跨分区精确重复修复

已复算开发集 10 个文件和 blind 293 个文件的 SHA256，确认 303 条记录共享同一个图像哈希。这里是一个重复图像组，而不是 303 个不同图像泄漏。排除开发侧全部 10 条记录，生成新的开发回放成员清单，共 15,575 帧；相对已提供的三个保护索引，文件 SHA256 交集为零。

产物目录：`data/research/ml_training_recovery_v1/replay-exact-clean-v1`。`report.json` 绑定输入、输出与保护索引哈希；`train_membership.jsonl` 为派生成员清单，图像相对路径以原 `visual_yolo_v2` 为根；`excluded.jsonl` 记录全部排除项。原数据和冻结基线保留，历史训练及 blind 指标不会因本次清单修复而自动恢复独立性。

复核发现上一轮报告有范围表述过强的问题：qualification receipt 明确只覆盖已发现的 v2_1 qualification，不能宣称全仓库保护池完整。原精确去重输出的 409 帧仅是相对旧 run18 批次的新增量；141 个旧批次重合帧并非语义不合格。重建训练池时，550 个已审核接受帧应按图像保留一份并沿用新审核结果，不能因为曾经训练过就永久删除。

本次完成的是跨分区文件精确重复修复。训练入口尚未切换到派生清单。后续近重复、跨编码相同像素检查、recording/seed 分组与最终配额核对仍需完成，训练准入保持 false。没有将感知哈希相近直接解释为已确认重复。

执行命令：`.venv/bin/python scripts/vision/repair_replay_exact_overlap.py`。

验证了保护集优先排除、批内确定性去重、报告身份及全部输出文件哈希。
