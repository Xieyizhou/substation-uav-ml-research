# 整图暂缓对照：生成与训练前核验

日期：2026-09-09。独立目录 `whole-image-hold-control-v1`。不启动训练，不解封新场景。

## 版本与采样

生成232张保留成员的独立引用清单，T08/T29/T30/T33整体暂缓，完整标签随图退出策略组。原236张及历史文件不修改。独立导出目录含两组所需原始成员副本；只有策略组的清单和加载器排除四图，参考组有意保留它们以形成对照。不能把共享导出目录直接作为策略组训练输入。

策略清单含388个标签：电抗器31、变压器149、开关柜168、电容器组40。T04/T26/T32的元数据语义说明记录在旁路清单，不覆盖历史字段。

两组各seed 7/17/27，均2700次曝光、450批次。参考组完全复现旧交错序列；策略组仅在四图曝光位置按同子集SHA-256确定性循环替换。基础池972、常规702、桥接540、困难负例486次曝光保持一致；每50步、每成员、每类实例和登记谱系的账本已冻结。

| seed | 替换位置数 | 电抗器实例曝光变化 | 变压器 | 开关柜 | 电容器组 |
|---|---:|---:|---:|---:|---:|
| 7 | 142 | -97 | -43 | +128 | -46 |
| 17 | 141 | -97 | -51 | +140 | -52 |
| 27 | 142 | -97 | -56 | +138 | -48 |

因此本轮是整图暂缓加替换策略对照，不是保持类别监督相同的标签质量对照。没有为抵消这些变化追加类别重采样。

原池236张不等于每个seed都曝光236张：参考组实际224张，策略组实际220张；两者均有108张负例实际曝光。零曝光成员不会被算作训练见过。登记谱系计数不是独立场景认证。

## 来源与就绪边界

重新核验原尺寸RGB与来源像素完全一致、完整标签坐标对应及登记关系闭包。基础池62张的历史实际标注模式缺失等既有缺口继续逐成员保存；本次不补发历史采集语义门禁证明。没有生成新的AI通过决定，仅绑定既有审核和七项修订提案。

生成初次检查遇到实现错误：追溯摘要中的annotation不含bbox字段。已在冻结任何清单前修正为读取唯一原始采集回执中的完整真值；独立generation-note留痕。这不是标签冲突，也没有修改历史标签。

六个配置使用既有真实dataset/loader工厂，逐一遍历2700张、450批；每个dataset校验完整标签，禁止创建优化器、反向传播和训练验证。最终完成情况、相关测试输出、v2.11固定40文件核验及状态以ready.json和verification.json为准；不声明全仓测试通过。

全部核验通过时状态为 `ready_for_training_not_started`，仅表示该开发数据策略对照具备训练输入条件，不表示历史风险全部解决，更不表示模型有效。所有产物保持 `training_admitted=false`、`promotable=false`。下一阶段显式训练入口必须读取此回执、重新核验协议/序列/权重与全部输入，使用同一工厂；不能绕过回执直接扫描共享导出目录。

评估继续固定48张配对图和48张无目标开发图、既定正式/诊断阈值及历史A和同预算retained_reference-450能力保留门槛。当前没有新模型或新评估成绩。
