# 监督风险：权限恢复后的两个试点复验

日期：2026-09-09。独立批次：`supervision-risk-permission-replay-v1`。

## 结果

**T30、T08 均已取得与原帧像素完全对齐的实例证据。** 原来的执行权限阻断已解除，七框修订提案更新为 `revision_proposal_ready_not_applied`。

新一轮最多各3次尝试，两个试点都在第1次完成；没有使用剩余预算扩展到其他帧。上一轮六次失败记录完整保留，不覆盖或隐去。不修改历史标签、不导出新训练池、不训练、不解封新场景。

## 权限和对齐

先做独立本地回环端口检查：沙箱内失败，获准的沙箱外检查成功。Gazebo 随后在获准权限下运行，使用独立通信分区及任务日志路径，没有改变设备、材质、光照、几何或保存的相机实际位姿；新世界仅增加已校验的诊断实例传感器。

| 核验 | T30 | T08 |
|---|---:|---:|
| 本轮尝试次数 | 1 | 1 |
| 原帧 RGB 像素完全一致 | 连续3帧 | 连续3帧 |
| 最大原始框坐标差 | 0像素 | 0像素 |
| 最大同步差 | 32毫秒 | 32毫秒 |
| 原尺寸目标实例像素数 | 41 | 4,241 |
| 可见区域框，原尺寸 xyxy | [0,428,6,435] | [0,368,131,415] |
| 本次进程清理 | 完成 | 完成 |

位姿与相机合法性门禁均通过。选择的是最早出现的内部稳定三帧（采集序号2、3、4），不是按最接近原图或审核结果挑帧。目标运行标签208在保存世界中唯一解析；T08 的身份重建仍不等于给历史采集补发新版门禁通过证明。

## 新增 AI 辅助审核

已经逐一查看全图实例叠图和目标局部。新审核不显示模型命中、置信度或 seed；此前看过模型结果，所以仍不称盲审。

- **T30：实例可见、内容不足。** 目标只在左图缘留下极小片段，不能据此辨识电抗器类别；完整框中大部分是前景设备。41像素是观察值，不是新设的收录阈值。
- **T08：实例可见、内容不足。** 后方目标只露出上部窄带，前景设备和浅色附件遮挡明显；不能把框内附件认作电抗器部件。

两个事件均建议在未来独立版本中**暂缓整张图**。掩码可见范围不替换 `full_2d` 完整框；组件归属仍为 `unknown`。这不是“原图没有目标”，也不是“框坐标已经证明错误”。

## 合并后的七框提案

| 处理建议 | 事件 |
|---|---|
| 整图暂缓提案，尚未执行 | T08、T29、T30、T33 |
| 仅元数据语义说明，保留原框 | T04、T26、T32 |

T29/T33 保留上一轮的原图与来源审核依据，没有将两个试点的掩码认证套用到它们；T32 继续引用已重验的同帧历史实例证据。七个事件的逐项依据和更新关系全部保留。

若未来执行四图暂缓，当前236张池在已登记关系闭包下会剩232张，但**本轮没有生成这个池**。除了4个电抗器框，还会连带移出7个变压器、3个开关柜和3个电容器组框。按既有 seed 7/17/27，受影响图像曝光为142/141/142次；不得悄悄补样以维持预算后声称只改了监督质量。

已登记关系没有扩展出额外成员，但成员级占位谱系仍不足以证明不存在其他同源关系。752项细结构待确认、其他历史来源元数据限制以及开发条件迁移问题没有因此被解决。

## 交付及下一判断点

主要产物为冻结协议、环境预检、两个完整重放回执及原始输出、新的逐图审核补充、合并七框提案、影响账本引用、测试和完成回执。所有产物为 `training_admitted=false`、`promotable=false`。

相关测试45项通过；v2.11固定40文件完整性通过；上一阶段绑定文件保持不变。未读取保护标签，未声明全仓测试通过。

下一步可以评审并制定独立暂缓数据版本及其可比较训练设计；在明确执行授权前，不移动成员、不改完整标签、不重分配曝光、不训练。监督风险证据已经增强，但不能承诺移出四图便能解决光照敏感或模型退化，更不能宣布找到唯一根因。
