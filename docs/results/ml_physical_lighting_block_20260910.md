# 物理光照对照：原条件重放中的完整标签阻断

## 判断

已执行五张原条件重放。L01–L04通过技术检查，尚不等于全图审核批准；L05触发完整标签成员冲突。五张光照变体均未采集，未执行新数据加载器预检或训练。不能签发训练就绪。

L05连续稳定帧2、3、4的RGB均与历史原图逐像素完全一致，实际位姿检查通过，各流相对RGB的最大时间差分别15、1、32毫秒。历史电抗器208框坐标差为0，没有丢失原框。重放另外检出实例64，唯一映射为transformer_mid，三个稳定帧均有24,494个实例可见像素，可见区域半开包围框为[1770,350,1920,989]。历史完整标签仅含电抗器，未包含此实例。

## 逐框AI辅助审核

已查看原图全景、原尺寸右边缘裁剪及实例掩码叠图。图缘可见很窄的深色竖直侧面与底部平台片段，主体大部分在画外；不依靠外形推断类别，类别与实例归属依据保存世界映射及对齐实例掩码。现有证据支持“实例有可见证据，但内容不足”。组件看起来是侧面及底座，但没有组件级ID，不认证具体组件身份。

新增full_2d框纵向覆盖整个画面，与实际可见区域不同。不得把几何框内所有背景当作实例像素，也不能据此直接补写训练框或自动忽略该实例。本结果确认全图监督存在遗漏风险，并不证明它是既有模型退化的唯一原因。

## 影响与停止原因

涉及L05／C31-L0，成员candidate:74a84f89ff66e645164692abb0d1e67fec055bb9081ee2188cc4b5b782d2849b。原方案每seed为此成员安排23次光照替换，占98次替换的一部分。直接删除该成员或补写标签都会改变冻结方案，因此本轮停止，未调整配额、阈值或历史数据。重复seed不是新独立场景。更广泛同源影响尚未完成来源级排查，不宣称风险仅限这一成员。

此前该帧已有标签的可辨识审核，不再足以支持“全图标签完整”；本文件是新增勘误，不重写旧审核，也不否定已标电抗器本身的拟合统计。

## 技术记录

初版同步计算错误地使用所有流的最大时间跨度。已在独立v2入口恢复既有“各流相对RGB”的33.334毫秒判定；数值阈值没有放宽。L01旧失败回执完整保留，使用同一份原始输出重新核验，并未为通过而重拍。L05属于语义冲突，不做技术重试。

相关36项回归通过；固定v2.11的40文件完整性及进程清理结果见绑定回执，不宣称全仓测试通过。所有新产物training_admitted=false、promotable=false。

## 下一优先方向

先制定独立的数据风险处理方案：追溯L05同源成员，明确极端截断实例的整图暂缓规则及影响范围，再决定是否冻结新的采集成员与曝光方案。当前不能直接沿用五帧计划训练，也不自动改为四帧对照。此次完成的是阻断定位，而不是所有监督问题的解决。
