# 统一暂缓后的双组光照对照：训练前就绪

状态由training-preflight/ready.json核验：ready_for_training_not_started。本文件只有在对应回执存在且输入哈希有效时才代表就绪；不代表训练或评估结果。

## 已完成

按新设计完成一次三seed计数求解，10张暂缓成员全部零曝光。三个seed的单图最大增量分别12、12、13，替换旧位置分别155、162、159；没有复用旧失效计数解或事后挑选等价解。

四源L01–L04实际配额为21、3、23、23，共70次／seed。L02由预定公式随新成员曝光降至3次，不人为补足75次。四张光照图均通过采集技术检查，全图与11个目标完成显式AI辅助审核；保留后方目标遮挡、右缘截断和未见面板限制。原／光照实例像素归属及框几何一致，仅RGB发生预期光照变化。

新R-clean与L-physical各包含seed 7、17、27，六个配置均冻结2700次曝光／450批。R-clean每seed使用214个来源成员，L-physical增加4份同源光照派生图文件，不能将其当作新增独立场景。两组从同一来源序列构造，固定70个位置仅变RGB图像，完整标签文件完全相同，负例位置与每位置亮度数组保持不变。

| seed | 电容器组实例曝光 | 电抗器 | 开关柜 | 变压器 |
| --- | ---: | ---: | ---: | ---: |
| 7 | 979 | 984 | 2399 | 2388 |
| 17 | 984 | 984 | 2402 | 2389 |
| 27 | 981 | 986 | 2405 | 2387 |

两组各seed以上计数完全相同，并等于冻结直接参考的对应计数。每50步账本及所有替换位置已落盘，不凭汇总数替代实际加载核验。

## 实际预检

六个真实数据集／加载器共遍历16200次加载、2700批。逐批核验完整cls、bboxes、batch_idx，两组监督张量完全一致；来源顺序、冻结亮度系数与日志一致，未替换位置的图像张量完全相同。记录实际增强前后像素哈希和批张量哈希。

预检期间阻止优化器创建、反向传播及YOLO训练／验证入口；日志中train字样是数据集模式名称，不代表训练。权重仍未训练。当前拟用CPU、640、batch/nbs=6、AdamW、LR=0.0005、450步、仅冻结亮度增强，六单元均从v2.11独立初始化。

原始文件、标签、权重、审核和协议未修改。新导出仅在独立目录内复制原图／原标签并增加4份受控光照图，标签不做数值修订。A16边界归一化仅作用于已记录独立诊断元数据，不静默改变训练标签。

## 验收与下一步边界

85项相关回归（含补偿约束与两阶段最优目标）及v2.11固定40文件完整性通过，不声明全仓测试通过。全部新产物training_admitted=false、promotable=false。

下一步须明确启动六个训练单元；开始时重验ready.json、输入、运行时和初始化身份，终点只使用last.pt。其后按冻结96张开发图执行正式／低阈值评估及错误审核。当前没有模型改善或候选达标结论，不解封新场景、不晋升。
