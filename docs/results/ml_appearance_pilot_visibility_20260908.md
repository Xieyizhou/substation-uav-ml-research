# 先导实例重放完成：全图监督筛选存在缺口

对 9 张待定帧、21 个柜体框完成逐帧重放。全部第一次技术尝试成功，连续三帧内部 RGB／实例掩码稳定，重放 RGB 与各自原帧逐像素一致，原有框与同步／位姿检查通过，进程正常清理。没有用同谱系另一帧代替。

## 逐框 AI 辅助结论

21 个框中，6 个确认实例可见且内容可辨识，15 个确认实例有可见证据但内容不足。所有框都有实例像素，没有“目标完全不存在”的结论。组件级掩码仍为 unknown，RGB 中的顶面／面板描述不构成组件级认证。

| 视角中的待核验实例 | 每个变体可见像素 | 三变体判断 |
| --- | ---: | --- |
| 开关柜视角：后排 west_switchgear_04 | 8,625 | 内容不足：主要顶面及窄上沿 |
| 开关柜视角：west_switchgear_03 | 41,541 | 内容可辨：较宽正面、顶面及部分面板 |
| 开关柜视角：近处 west_switchgear_02 | 282,920 | 内容不足：大面积顶面，主体／面板大部出画 |
| 电抗器视角：边缘 west_switchgear_04 | 56,622 | 内容可辨：连续侧面、转角与基座 |
| 电抗器视角：switchgear_west | 300,021 | 内容不足：几乎仅顶面 |
| 变压器视角：west_switchgear_01 | 504 | 内容不足：遮挡缝隙窄竖条 |
| 变压器视角：switchgear_west | 92 | 内容不足：前景边缘极小片段 |

这些是相同几何的三种视觉条件，不是 21 个独立场景。判断基于逐框内容而非面积阈值：大面积顶面同样可能缺少可辨内容；不能把像素数直接转成准入规则。

## 已证实与未证实

已证实：这些框的仿真实例归属可解析，采集与重放对齐正确；看起来落在前景设备上的两个框确有后方实例碎片。这不是标签映射碰撞或同步漂移。

已证实的流程缺口：候选视角通过计划目标中心遮挡筛选和相机合法性，不等于全图每个带标签设备都可作为充分监督。此前通过的几何预检不能替代全图实例内容审核。

尚未证实：这些风险样本是否是所有模型未达标的唯一原因；当前已有模型对材质／光照的敏感性也涉及类别混淆和能力取舍。不能由本检测直接断言模型结构有错。

## 下一步边界

本先导继续不准入，保留全部原图、标签和三个变体，不扩采剩余 16 张，不以删除难框或裁掉风险区域修饰结果。对新候选修正筛选流程：先冻结有界、独立的新位姿清单，先采原始外观，审核全图所有监督实例；有疑问先做精确对齐实例重放。只有完整视角通过后才渲染材质与光照变体，再逐帧审核。计划目标与非计划实例使用同一内容要求。

若当前布局在有界搜索中无法提供足够合格视角，应报告覆盖缺口并另行设计明确登记的新训练布局／资产，不能持续放宽可见性要求。封存的新场景测试仍不访问。

产物位于 `appearance-recovery-pilot-v1/instance-replay-v1`：冻结协议、9 份重放回执、四张实例叠色审核页、`decisions.json`。26 项相关测试通过，v2.11 固定 40 文件完整性复核通过；未声明全仓测试通过。所有新产物 `training_admitted=false`、`promotable=false`。检测完成不等于先导通过，更不等于模型达标。
