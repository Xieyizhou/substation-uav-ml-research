# 迁移条件替换：计数可行性核验

以已完成的低曝光 VM 三 seed 实际冻结序列为起点，四个灰色诊断条件均可构造固定预算替换：每 seed 保留 2670 个位置，只替换原 24 张暖／冷变体所在的 30 次曝光。

每个待替换成员通过原图哈希、完整真值及唯一仿真实例映射，核对到同源原始位姿。完整类别实例数量相等；不涉及负例位置，位置上的亮度系数不重抽。每个普通来源占 2 次，S07 占 8 次，集中度未改变。

这是符号序列可行性，不是训练就绪回执。真实灰色图仍须完成采集、全标签审核、标签导出一致性和真实加载器预检。所有四条件均检查可行性，尚未根据先导成绩挑选训练条件。

科学解释范围：把暖／冷条件替换为某个灰色条件，而非增加同一批图的曝光，也不是仅调整单一色相。共享布局、资产和位姿；无法据此声称独立场景泛化。保留既有风险与未确认项，不重新批准历史全池。

机器可核验结果：`condition-transfer-route-v1/factorial-probe-v1/symbolic-replacement-feasibility.json`，包含三个 seed、四条件的精确符号序列、原位置、原成员、类别实例向量与冻结亮度系数。

当前未训练、未收录或晋升。最终条件选择仍等待全 12 个来源位姿的诊断结果与质量门禁。
