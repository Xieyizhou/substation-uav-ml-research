# 电容器外壳遮挡：实际重放与组件诊断

## 已完成的重要判断

本轮完成一张冻结训练来源帧的原样重放控制及外壳 visual 移除诊断，各一次技术尝试成功。**在本帧中，实体外壳确实遮住了内部可辨圆柱细结构。** 这是实际渲染干预结果，不再仅是 XML 几何推断。

本结论不证明这是模型漏检的唯一原因，也不证明修改资产后模型一定提高。未训练、未推理、未修改历史标签或世界、未解封新场景，诊断图保持不可训练收录、不可晋升。

## 固定来源与范围

从上一轮 49 个目标中筛选：电容器、来源无具名缺口、已有条件审核非待定、无明显前景遮挡、三个 seed 均有实际曝光；按成员 ID 排序选第一个，不使用模型成绩选图。

固定目标 T020，成员 `bridge:06efbbf762bc79f7e644c3d4187ece6d4686aef25ade21e831905baf943fb513`，来自 `background_bridge` 保存训练来源，设备为 `capacitor_east`，不是封存测试图。仅此一帧，不把三帧稳定输出当作三个独立样本。

本轮使用独立扁平目录 `data/research/ml_training_recovery_v1/asset-visibility-replay-v1`。原计划继续嵌套在历史目录时触及 macOS 路径长度限制，尚未启动模拟器即失败；改用短目录后，以冻结输入哈希保留来源关系，没有移动或覆盖历史产物。

## 原样重放控制

仅增加与 RGB 同配置的实例分割传感器，按保存的实际载体位姿重放；原几何、材质、背景、光照和已有传感器保持不变。

- 连续三帧稳定 RGB 和实例掩码，按首次内部稳定窗口选择，不按与原图的相似程度挑帧。
- 三帧 RGB 与原图逐像素完全一致，差异像素均为 0。
- 对应全图实例框最大坐标差均为 0 像素。
- 同步差分别约 5、32、1 毫秒，均小于等于 33.334 毫秒。
- 保存实际位姿误差门禁、光心与载体地图/扩张障碍检查通过。
- 设备实例聚合掩码为 44,125 像素，三帧一致；这不是圆柱组件掩码。
- 本次启动进程已清理。

## 诊断干预

控制通过后另冻结协议，仅在副本中移除 `capacitor_east/link/visual[name=body]`。不移除 collision，不移动六个圆柱，不改变基座、剩余 visual 标签、材质、设备、相机或光照。结构差异验证拒绝任何额外改动。

诊断输出也满足三帧内部稳定、位姿及同步门禁；同步差约 24、32、1 毫秒。设备聚合掩码为 19,439 像素，三帧一致。相对原图的全图差异为 42,059 像素，包含露出的背景及随外壳移除改变的遮挡/阴影，不能当作圆柱可见面积。

干预改变了可见几何，因此不要求受影响目标的框与原图相同；完整原始标签不能作为诊断新图的真值，也不认证新图为原帧像素证据。原样控制的严格门禁没有被放宽。

## 本轮 AI 辅助审核

重新查看了原样全图、原目标裁剪、原实例叠图，以及诊断全图和同坐标裁剪。

原图目标为宽阔蓝绿色实心平面与黑色基座，没有显露可辨浅色圆柱。移除外壳 visual 后，基座上方出现三列可辨的浅色竖直圆柱轮廓，原先被挡住的后方建筑与地面也显露。与冻结的单一组件移除相结合，支持本帧中的外壳遮挡解释。

图像上不能分别确认六个圆柱；前后圆柱会相互遮挡或投影重叠。现有掩码按设备聚合，不认证每个组件的像素数或独立可见性。此审核只形成诊断观察，不形成训练质量批准。

原图目标裁剪：

原样控制目标（本地研究资产：`data/research/ml_training_recovery_v1/asset-visibility-replay-v1/body-visual-removal-v1/replay/T020/attempt-01/stable-1-original-crop.png`）

仅移除外壳 visual 后：

移除外壳后的诊断目标（本地研究资产：`data/research/ml_training_recovery_v1/asset-visibility-replay-v1/body-visual-removal-v1/replay/T020/attempt-01/stable-1-crop.png`）

## 下一步出口

优先制定**独立资产修订方案**：先确认预期设备形态究竟是封闭箱体式电容器，还是应显露电容器单元的开放式电容器组。不能仅为提高识别率，把可能合理的设备外壳当作实现错误直接删除。

若预期为开放式结构，应在新资产版本中调整外壳/支架关系并重新采集、审核和冻结标签，不覆盖旧世界；随后再独立比较几何可辨细节与材质覆盖。若预期本来就是封闭箱体，则保留外壳，优先开展主体材质及与普通柜体的困难区分对照。二者不能混为单变量训练。

本轮不自动执行上述修订，不将诊断副本纳入训练；此前历史来源缺口和待定框仍然保留。

## 验证

31 项相关回归通过，覆盖组件移除白名单、传感器一致性、来源选择、几何包含、实例掩码、精确对齐、缺失/过期审核与输入、进程取消/超时清理。v2.11 固定 40 文件完整性核验通过，不声明全仓测试通过。

冻结协议、技术回执、输出、人工编写的 AI 辅助观察及中文报告由完成回执绑定；恢复重新校验，不启动新的训练或重复有效重放。
