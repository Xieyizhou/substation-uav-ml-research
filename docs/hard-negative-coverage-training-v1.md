# 困难负例组成对照：预先固定的训练规则

2026-09-07，补采进行中、未运行本批模型推理时登记。独立产物在 `hard-negative-coverage-v1/training-v1`。

- O：既有 24 张审核负例。N：既有 24 张加本批 96 张通过审核负例。两组共同正样本为基础 66、常规 48、桥接 48 张。
- 每组 100 个优化步，batch 6，seed 7/17/27，共六个独立训练单元，均从冻结 v2.11 初始化。不是延续旧 checkpoint。
- 每单元确切 600 次曝光：基础 216、常规 156、桥接 120、负例 108。正样本身份、顺序和全部曝光位置完全复用对应 seed 的冻结 Y-100 序列；负例只在原来的 108 个位置替换。
- O 的负例序列也复用对应 Y-100。N 以固定 seed 洗牌 60 个位姿对，选取前 54 对、两光照各一次，共 108 次，再固定洗牌投放到负例位置。选择不读取模型成绩。每 seed 只使用 54 对，另 6 对零曝光必须列示，不声称每个 seed 使用全部 120 张；跨 seed 重复不是新独立样本。
- 本轮不加入类别重采样，不同时变更负例总配额和优化预算。N 与 O 回答负例来源池扩展及组内曝光重新分配的组合效果，不是纯新增曝光效应。
- CPU、640、AdamW、lr=0.001、无预热、恒定学习率、关闭原有全部增强、workers=0、nbs=6；只用预定终点 last.pt。保留实际成员/类别实例/谱系曝光与损失曲线。训练成员验证仅称拟合诊断。
- 未完成 96 张全图审核、完整 48 对、来源及指纹检查时不冻结训练成员、不启动训练。相似性待核验不得自动通过；不按误检触发与否挑选图片。
- 断点复用先复核全部输入/审核/实际曝光/权重。失败尝试保留，新尝试独立目录，最多三次技术尝试；语义错误不得重试绕过。
- 正式评估仍为旧 48 张配对开发图和 48 张无目标开发图，CPU/640/conf=0.37/类别相关 NMS IoU=0.7/max_det=300/同类一对一匹配 IoU≥0.5。开发集不训练、新场景不解封。
- 保留既有计划命中及 FPR 门槛。原始和光照全图/逐类三 seed 平均召回相对同预算 R-100 与历史 A 均下降不超过 0.05，同时报告相对 O 的变化。未全部通过不选择开发候选。
- 用户本次要求推进至训练完成。训练完成与评估通过分开记录；未运行评估时不得声称获得候选或模型改善。所有产物 `training_admitted=false`、`promotable=false`。
